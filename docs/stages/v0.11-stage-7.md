### 阶段 7：全局 Review

> ✅ **已完成**（v0.11.0 阶段 7 会话产出，2026-06-30；docs/reviews/REVIEW-13.md）

> **独立会话指令**：`阅读 CONTEXT.md、docs/stages/v0.11-stage-7.md，对 v0.11.0 cycle 阶段 1-6 全部产出进行 Code Review，产出 docs/reviews/REVIEW-13.md，本阶段不动任何代码`

**目标**：对 v0.11.0 cycle 阶段 1-6 全部产出进行全局审查，产出结构化问题清单交给下一阶段修复。本阶段**禁止修改任何代码**（git diff 应为空，除新增的 REVIEW 文件外）。

**前置依赖**：阶段 1-6 全部 `[x]`。

**依赖测试**（开工时跑这些测试的详情）：
- 全量回归（看数字）：`cargo test --release -q`
- 基线三件套：fmt + clippy + no-default-features build

**任务清单**：

1. **基线测试三件套**（确认无回归）
   - `cargo test --release -q` ≥ 1008 passed / 0 failed（v0.11.0 cycle 累计）
   - `cargo fmt --all -- --check` 通过
   - `cargo clippy --release --all-targets -- -D warnings` 0 warnings
   - `cargo build --release --no-default-features` 通过
   - 任一失败 → 立即标 P0 + 列具体错误到 REVIEW-13.md

2. **代码质量审查**
   - worker restart 实装是否引入死锁 / race condition（restart_history 共享状态访问安全）
   - DNS ETW worker 是否正确处理 callback 内 panic（避免 ProcessTrace 线程崩溃）
   - FilterExpr v2 是否破坏 v0.7/v0.8 现有表达式语义
   - 签名验证 BackgroundScorer 集成是否阻塞主线程 50ms tick
   - parent_chain 构建是否防循环（PID 复用场景）
   - R16/R17/R18 规则扣分是否过严（误报率）

3. **架构审查**
   - 4 个新模块（signature.rs / lineage.rs / path_rules.rs / dns_log/etw.rs）是否符合 ADR-0015/0018/0019/0020/0021 的设计
   - 是否引入不必要的依赖（v0.11.0 应 0 个新依赖进默认依赖图）
   - ProcessInfo 字段扩展是否破坏 serde round-trip（旧录屏兼容性）

4. **安全性审查**
   - worker restart 是否被恶意触发（如外部输入触发 panic 后消耗资源）
   - DNS ETW worker 是否记录敏感查询（如内网域名查询不应写日志）
   - 签名验证是否泄漏文件路径信息
   - lineage_rules.toml / path_rules.toml 用户配置是否安全（TOML 解析拒绝恶意输入）

5. **跨平台审查**
   - cfg-gate 是否正确（DNS ETW / WinVerifyTrust 在非 Windows 上 stub）
   - `cargo build --release --no-default-features` 是否仍通过（验证 cfg-gate）
   - Linux 上 schannel_etw_worker / dns_log etw 路径是否正确为 None

6. **性能审查**
   - 签名验证缓存命中率（BackgroundScorer 不应每 heavy refresh 都全量重验）
   - parent_chain 构建复杂度（O(N * depth)，N = 进程数，depth ≤ 32）
   - DNS ETW callback 频率是否影响主线程（应仅 worker 线程开销）
   - worker restart 退避策略是否合理（避免 panic loop）

7. **完整性检查**
   - plan.md 中所有功能阶段已 `[x]`（阶段 1-6）
   - 所有测试通过
   - 文档完整：ADR-0019/0020/0021 + tech-debt.md TD-4 标 Fixed + CONTEXT.md 新术语
   - 6 个 stage docs（v0.11-stage-1~6）头部 ✅ 已发布标记

8. **产出 `docs/reviews/REVIEW-13.md`**，按下方格式：

   ```markdown
   # 全局 Review 报告 — v0.11.0 cycle（阶段 7）

   **审查范围**：v0.11.0 cycle 阶段 1-6 全部产出
   **审查日期**：YYYY-MM-DD
   **审查人**：stage 7 会话
   **基线测试**：`cargo test --release -q` = N passed / 0 failed / 3 ignored

   ## P0（阻断性，必须修才能交付 v0.11.0）
   - [问题描述] — `文件路径:行号` — 建议修复方式

   ## P1（重要，影响质量）
   - [问题描述] — `文件路径:行号` — 建议修复方式

   ## P2（建议，长期改善，归档到 tech-debt）
   - [问题描述] — 影响评估

   ## 审查覆盖矩阵（按 stage 7 doc 任务清单 7 子项）
   [矩阵表格]

   ## 验收对照（plan.md 阶段总览 vs 实际）
   [对照表]
   ```

**验收标准**：
- `docs/reviews/REVIEW-13.md` 已产出，按 P0/P1/P2 三档分级
- 本阶段未修改任何代码（git diff 应为空，除新增的 REVIEW 文件外）
- 全量回归通过（无回归）
- 基线三件套全过

**容量预估**：0 行代码 + 1 个 REVIEW-13.md（~300-500 行 markdown），工具调用 30-50 次（读代码 + 跑测试 + 写报告）。

---

**下一阶段启动指令包**（完工后输出）：

```
═══════════════════════════════════════════════════
▶▶▶ 下一阶段启动指令（请复制以下整段到新对话）

阶段 7（全局 Review）已完成。docs/reviews/REVIEW-13.md 已产出。

请开启新对话，粘贴以下指令开始阶段 8（批量修复与收尾交付）：

---

阅读以下文件后开始：

【项目宪法】
- CONTEXT.md
- docs/stages/v0.11-stage-8.md
- docs/reviews/REVIEW-13.md（本阶段产出，含 P0/P1 问题清单）

【本阶段全部产出】
- docs/reviews/REVIEW-13.md — Review 报告（P0 0 / P1 N / P2 M）

【开工时执行的命令】
- cargo test --release -q 2>&1 | grep "^test result:" | awk '{p+=$4; f+=$6; i+=$8} END {print p" passed / "f" failed / "i" ignored"}'  （应 ≥ 1008 passed）
- cargo fmt --all -- --check
- cargo clippy --release --all-targets -- -D warnings
- cargo build --release --no-default-features

完成所有任务后确认完成，并输出最终交付物清单（无下一阶段）。

---

═══════════════════════════════════════════════════
```
