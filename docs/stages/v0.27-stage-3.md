# 阶段 3：Slice + Review/收尾 — 远端验证与 cycle 完结

> **独立会话指令**：`阅读 CONTEXT.md 和 docs/stages/v0.27-brainstorm.md（重点：3 stage 总览 stage 3 行 + 已知风险段 + 验证矩阵「远端 CI 绿」锚 + FAQ Q5）+ docs/adr/0037-ci-recovery.md（D1-D5 + stage 2 实装注记）+ docs/stages/v0.27-stage-1.md（附录 B.4/B.5 CI 可跑类与时序观察类清单）+ docs/stages/v0.27-stage-2.md（完工验证段——远端验证的本地侧基线），按手册「开工前必做 8 步」验证基线（回归双档 1744/0/10 + 1768/0/11 各 80 行 + 三件套；首跑红按 TD-62 口径单跑确认后复跑）后，先产出本文件再执行`
>
> **开工前只需阅读**：
> - `CONTEXT.md`（领域词汇表，私有 .gitignore 文件）
> - `docs/stages/v0.27-brainstorm.md`（cycle 总览 + 5 决策 + 执行手册 + 验证矩阵 + FAQ Q5）
> - `docs/adr/0037-ci-recovery.md`（D1~D5 决策集 + stage 2 实装注记——远端验证的对象就是这些实装）
> - `docs/stages/v0.27-stage-1.md`（附录 B.4 CI 可跑类 / B.5 时序断言观察类——观察清单）
> - `docs/stages/v0.27-stage-2.md`（完工验证段——本地侧已验证基线 + 已知风险 4 条）
>
> **stage 3 追加阅读**（Review 阶段需要）：`docs/reviews/REVIEW-v0.26.md`（报告格式模板）+ `docs/stages/v0.26-stage-2.md`「required checks 设置说明」段（本阶段任务 4 的更新对象）

---

## 目标

CI 修复专项的**最终防线验证 + cycle 收尾**：用户拍板 push `398731e` → 远端观察 ci.yml 三 job（check / msrv / audit——release 仅 tag 触发不跑）→ 红 job 按「取证-修复循环」迭代（预算内）→ 全绿后 badge 激活 + required checks 设置落地 → REVIEW-v0.27 + CHANGELOG 0.27.0 + Cargo bump 0.26.0→0.27.0 + tag `v0.27.0`（用户拍板项）+ v0.28+ 候选评估收官。

**Slice + Review 完成后状态**：
- 远端 ci.yml 三 job 全绿实证（run URL + job 级结论留档）；tag `v0.27.0` 的 release run 绿（build job，winget job 已删）
- README badge 从注释占位变实挂（URL 200 + SVG 内容显示 passing）
- 回归双档与开工基线一致（stage 3 若有红 job 修复，按实际变更重跑对应档并记录）

---

## 前置依赖

- stage 1 / stage 2 已 `[x]`（commit `8819ae2`（plan）+ `6abdb85`（stage 1）+ `398731e`（stage 2），均**未 push**——push 属本阶段任务 1 用户拍板项）
- **push 前尾注清理（用户拍板 2026-09-04）**：GitHub Contributors 面板出现 Anthropic `claude` 账号（130 个历史 commit 带 `Co-Authored-By: Claude` 尾注被新版 UI 计入 co-author 贡献者）——用户确认 Alfroul 即本人（121 direct 贡献归因正确），拍板：后续 commit 一律不带尾注 + 未 push 的本地 commit 尾注清掉再推（filter-branch 重写三个 commit，tree 零变化、作者/日期保留；历史已 push 的 130 处不动）
- 开工基线（本会话 2026-09-04 实测）：
  - 默认档 **1744 / 0 / 10（80 行核对）**
  - anthropic 档 **1768 / 0 / 11（80 行核对）**
  - 三件套：fmt / clippy 双档 / build 双档 / bench --no-run 全过
  - 不变锚：MCP tool 46 / agent catalog 47
  - git 干净（HEAD `398731e`，领先 origin/master（`e77901d`）三个 commit；eval log 本地留存惯例）；无 checkpoint 残留
  - Cargo.toml `0.26.0` ✓（cycle 版本号检查——本阶段收尾段 bump `0.27.0`）
- gh CLI 已认证（repo + workflow scope，2026-09-04 实测）

## 依赖测试

- 全量回归双档（若红 job 修复触碰 src / tests，完工按变更面重跑对应档；纯 workflow 修复零重跑）
- 远端 CI 三 job 是本阶段核心验收锚（本地无法替代）

---

## 任务清单

### 1. push 拍板 + 远端观察（用户拍板项）

- **push 内容**：`git push origin master`（`8819ae2` + `6abdb85` + `398731e` 三个 commit——brainstorm 定稿 + stage 1 Spike doc + stage 2 修复主项，尾注已清）
- **触发**：ci.yml（check / msrv / audit 三 job）；release.yml 仅 tag 触发不跑；miri.yml 已删除
- **观察方法**：`gh run list --workflow=ci.yml --limit 3` 定位 run → `gh run view <id> --json jobs` 逐 job 轮询（check job 预计 ≤ 60 min：fmt/clippy/build ~10 min + test 全量预留 45 min；msrv ~5 min（windows runner 首次无缓存偏慢）；audit ~3 min）
- **观察项**（stage-1 附录 B.5 + stage-2 已知风险 1）：时序断言类（`test_perf_baseline` <5ms / `test_search_perf` / scorer sleep 类）在 runner 慢硬件的行为 + `test_kill_by_name_finds_spawned_process` 降级路径（6 月 CI 失败史）——若红属风险 1 预案范围非失败

### 2. 红 job 取证-修复循环（stage 3 迭代预算）

- 红-job 处置流程：`gh run view <id> --log-failed` 取证 → 定位（新挂点 / 时序断言 / 权限类）→ 修复（最小面：优先 env-gate / 断言放宽按 TD-62 家族口径单独评估）→ 本地对应验证（改动面决定档位）→ commit + push → 复观察
- **修复纪律**：每轮迭代记入本文件「远端验证记录」段（run ID + 失败 job + 根因 + 修复 commit）；连续 2 轮同点不绿则停下来向用户汇报（超出预算判断点）
- **推定已知绿**：choke-point gate（CI=true→Unknown）覆盖的 WinVerifyTrust 整类挂点不会再挂；ETW 两文件已 skip；timeout 60/45 保证最多 45 min 确定性失败

### 3. badge 激活（全绿后）

- `README.md:7-10`：删注释首行（:7「CI badge: 挂接待远端……」）与末行 `-->`（:10），保留两条 badge 行（:8 CI / :9 release）——占位语法零重写
- **badge URL 200 核验**：`curl -sI` 两个 badge.svg URL + **SVG 内容检查**（拉取 badge SVG 确认显示 `passing`——URL 200 但内容 failing 的半绿状态不算兑现）
- 注意：badge 激活 edit 所在 commit push 后会再触发一轮 CI——badge 反映的是该轮 run 的状态，需这轮也绿

### 4. required checks 设置落地（v0.26 stage-2 doc 说明的兑现）

- GitHub 仓库 Settings → Branches → Add branch protection rule（`master`）→ Require status checks to pass before merging → 添加 **3 个 job**（job 结构已变，无 check-macos / miri）：
  - `check`（windows-latest：fmt + clippy + build ×2 + test 全量）
  - `msrv`（windows-latest + Rust 1.88：check no-default-features）
  - `audit`（cargo-audit + RUSTSEC-2026-0189 ignore）
- **手动设置项**（需仓库 admin + job 至少跑过一次才出现在搜索框；Claude 无法代办——v0.26 stage-2 doc 同款口径）：本文件留档更新后步骤，用户按需设置；设置完成后用户口头确认，REVIEW-v0.27 如实记录「设置说明已兑现/待用户操作」

### 5. REVIEW-v0.27（验收锚复核 + 溯源抽查）

- 新建 `docs/reviews/REVIEW-v0.27.md`（格式对齐 REVIEW-v0.26）：验收锚三项——**远端全绿实证**（run URL + 逐 job 结论 + badge SVG passing 证据）/ **回归双档不变**（或按修复面如实记录）/ **不变锚**（MCP tool 46 / catalog 47 / runtime deps +0）
- 溯源抽查：stage 2 commit message 数字 vs stage-1 附录（≥10 项门槛按 REVIEW-v0.26 口径）；本阶段远端 run 数据逐条留档
- Findings 分级（P0/P1/P2）+ §6 v0.28+ 候选评估（见任务 7）

### 6. CHANGELOG 0.27.0 + Cargo bump + tag `v0.27.0`（用户拍板项）

- `CHANGELOG.md`：`[Unreleased]` → `[0.27.0] - 2026-09-XX` + cycle 完结总结条目（对齐 0.26.0 格式：cycle 定位一句话 + 六组打包交付清单 + 验收锚终值）+ 补 stage 3 条目（远端验证记录 + badge + required checks + 亮点条收尾态数字）
- `Cargo.toml` `0.26.0` → `0.27.0`
- README 收尾态数字核对（25→26 release tags / ADR·TD 计数 / src·tests 行数 as-of 注记——按实际重测）
- **tag `v0.27.0`**：用户拍板后 `git tag v0.27.0 && git push origin v0.27.0`——触发 release.yml（build job 预期绿 10/10 延续；winget job 已删）；release run 绿 + GitHub Release 资产在位核验
- commit 拆分建议：badge + REVIEW + CHANGELOG + bump 一个收尾 commit（push 后最后一轮 CI 绿即 badge passing）；tag 指向该 commit

### 7. cycle 收尾（手册步 16-23 + v0.28+ 候选评估）

- brainstorm 3 stage 总览表勾阶段 3 `[x]`（唯一勾选点）+ brainstorm 备注 cycle 完结
- CONTEXT.md：v0.27 术语段收尾注记（badge 实挂 / required checks 状态）
- ADR-0037 stage 3 实装注记（远端验证结果回填）
- TD 台账如有新增观察项回填（B.5 时序类若触发按实际归档）
- **v0.28+ 候选评估**（brainstorm 备注预告项）：附录 A 模型组合（2026-11+ 秋招后重评）/ Multi-agent 方向 D（依赖附录 A）/ TD-51 MonitorManager 持久化 · TD-31 FilterExpr 跨 ctx · TD-49 replay 增强 / NT SAFETY 93 处专项 / TD-63 rmcp major / let-chain 现代化 37 处（stage 2 过渡 allow 的偿还）——按当时语境排序建议

---

## 远端验证记录（执行时回填）

**R1（首次 push 验证）**：push `e77901d..398731e`（2026-09-04 01:53 UTC，尾注已清的三 commit）触发 run `33827454883`，02:26 UTC 完成——**msrv ✅ / audit ✅（D2/D4 双修一次过；audit 6 warnings 为 warning 级 advisory 不挡）/ check ❌ 在 clippy 步**（fmt 已过；build/test 未达——clippy 挡路测试步本轮未执行）。

**R1 修复（迭代 1）**：远端 stable clippy 1.98 新 lint **`chunks_exact_to_as_chunks`** 单点——`src/inspect/env.rs:170` `.chunks_exact(2)` → `.as_chunks::<2>().0.iter()`（clippy 官方建议写法；`as_chunks` 1.88 稳定 API，msrv job 1.88 编译通过的同款前提；全仓 grep 唯一挂点）。本地复现路径：rsproxy 装 1.98.0 工具链（19s 级）→ `cargo +1.98.0 clippy --all-targets -- -D warnings` 复现同款单错误 → 修复后**全 target 绿**（lib+tests+benches 一次收敛，无第二批）；本地 1.95 clippy 双档绿 + fmt 绿 + 覆盖测试 3 binary 绿（test_inspect 11 / test_inspector 12 / test_env_mask 39 passed）。语义零变化（chunks_exact 本就丢弃余数字节，as_chunks().0 同款），v0.26 stage 4 clippy 漂移搭车修同款先例口径。commit `575ed91`。

**R2（迭代 1 验证）**：run `33829331943`（02:23 UTC 起）——clippy ✅（1.98 修复生效）+ build ×2 ✅ + msrv ✅ + audit ✅，但 **check job 60 min 上限取消**：test 步 02:40 起 43.5 min 全部耗在**冷缓存编译**（proc 自身 79 个 test target 的 release codegen 未完成，**零测试执行**；D1.4 预算「build ~10 min + test ~45 min」实测失真——build 前置实为 ~17 min，且 test 步含全量编译段）。取证：job log 时间线 + **本地 `cargo clean -p proc` + `cargo test --release --no-run` 实测 949s**（proc 自身 test target 冷编译；runner 4 核推算 32-63 min）。判定为**慢不是挂**（无 WinVerifyTrust 类挂点特征——env-gate 已单点覆盖且日志无 running-over-60s 报告）。

**R2 修复（迭代 2，workflow-only）**：check job `timeout-minutes` 60→**180** + test 步 45→**170**（覆盖冷缓存全流程 61-111 min 实测估算带）+ rust-cache **`cache-on-failure: true`**（红/取消 run 也保存编译进度，迭代期跨 run 累积）。命令本身零改动（`cargo test --release` 与本地同口径；`--tests` 范围等价性已验证——79 executable 同集，不缩面）。

**R3（迭代 2 验证）**：run `33861156088`（10:00:53–11:11:05 UTC，~70 min）——timeout 生效不再取消；clippy/build×2/msrv/audit 全 ✅；test 步编译段完成（rust-cache 首次成功保存）进入执行段，**首个真实测试失败**：`tests/test_disk_io_etw.rs::try_spawn_returns_some_on_admin_or_none_on_user` 断言「刚 spawn 不应有 poll」失败（实得 5）——**stage-1 附录 B.3 误分类修正**：该文件按头注释归类「CI 走降级路径」，但 GitHub runner 提权使 ETW 真启动走真实路径（与 D1.2 gate 的 dns/schannel 同族），硬时序断言环境敏感。本地全量双档终值同步完成（1744/0/10 + 1768/0/11 各 80 行；默认档首跑 TD-62 第 3 次触发，单跑绿 + 复跑绿归档）。

**R3 修复（迭代 3）**：`test_disk_io_etw.rs` 加 `ci_env()` gate——**仅 gate 失败的 `try_spawn_returns_some_on_admin_or_none_on_user`**（真实 ETW 硬时序断言，CI 价值零）；`spawn_collects_self_io_when_admin` **不 gate**（断言宽松设计上不 fail，且 runner 上提供真实 ETW 冒烟信号，R3 实测通过）；`disk_io_stats_shape` 契约测试继续跑。本地验证：无 CI 三测试照跑（行为零变化）+ `CI=true` 模拟 gate 触发 SKIP 实证 + fmt/1.98 clippy 绿。

---

## 验证

- ✅ 开工基线：回归双档 1744/0/10 + 1768/0/11（各 80 行）+ 三件套 + 不变锚 46/47
- 远端：ci.yml 三 job 全绿（run URL 留档）+ tag 后 release run 绿 + badge SVG passing
- 收尾：CHANGELOG/Cargo/tag/REVIEW 齐套；git 干净（eval log 除外）

## 完工收尾（按手册步 16-23）

- [ ] brainstorm 3 stage 总览表勾阶段 3 `[x]`（唯一勾选点）
- [ ] CHANGELOG `[Unreleased]` → `[0.27.0]` + stage 3 条目 + cycle 总结
- [ ] CONTEXT.md v0.27 术语段收尾注记
- [ ] ADR-0037 stage 3 实装注记
- [ ] commit（模板：`docs(v0.27-stage-3): 远端验证与 cycle 完结——CI 三 job 全绿实证（run URL）+ badge 激活（URL 200 + SVG passing）+ required checks 设置说明兑现 + REVIEW-v0.27 + CHANGELOG 0.27.0 + Cargo 0.26.0→0.27.0 + tag v0.27.0（用户拍板）+ v0.28+ 候选评估；回归双档 [终值] + 不变锚 46/47`）
- [ ] 输出阶段完成报告 + cycle 完结报告（无下一阶段启动指令包——cycle 收官；v0.28 brainstorm 另开会话）

## 已知风险（本阶段观察）

1. **首验红 job 属预期范围**（brainstorm 风险 1 + stage-2 已知风险 1）：B.5 时序断言类 + kill_by_name 降级路径是观察项；红即按任务 2 循环处理，不视为阶段失败。
2. **push 中间态红观感**（brainstorm 风险 6）：badge 只在全绿后激活，README 无断言远端状态的既有纪律延续。
3. **check job 时长不确定**：首次 push 无 rust-cache，build ×2 + test 全量可能贴近 60 min job 上限；若超时被杀优先考虑 cache 预热后的第二次 run 再判断。
4. **release badge 语义**：release.yml 仅 tag 触发，badge 反映最近一次 run（v0.26.0 的 winget 红）——tag `v0.27.0` push 后新 run 绿才转 passing；若 badge 持续显示旧态需核实 badge 分支参数语义并如实记录。
