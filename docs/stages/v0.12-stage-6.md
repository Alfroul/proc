### 阶段 6：Review + 批量修复与收尾交付（合并，Slice=4 触发合并规则）

> ✅ **已完成**（v0.12.0 阶段 6 会话产出，2026-07-04）

> **独立会话指令**：`阅读 CONTEXT.md、docs/stages/v0.12-stage-6.md，对 v0.12.0 cycle 阶段 1-5 全部产出进行 Code Review，产出 docs/reviews/REVIEW-14.md，修 P0/P1，完成 v0.12.0 交付`

**目标**：v0.12.0 cycle 阶段 1-5 全部产出全局 Review + 修 P0/P1 + tag v0.12.0。本周期 Slice=4，按 phased-project skill 规则合并 Review + 批量修复为单阶段。

**前置依赖**：阶段 1-5 全部完成。

**依赖测试**（开工时跑这些测试的详情）：
- `cargo test --release -q`（summary，应 ≥ 1130 passed）
- 基线三件套（fmt / clippy / no-default-features build）

**任务清单**：

1. **全局 Review（产出 `docs/reviews/REVIEW-14.md`）**
   - 代码质量审查：
     - Linux 代码移除是否干净（grep `EBPF_ENABLED` / `target_os="linux"` 应空）
     - ProcessFlow 简化是否破坏 serde round-trip（旧录屏 v0.11 含 `source: "ebpf"` 字段，反序列化应不挂）
     - SignatureStatus 9 状态机 + trusted_signers.toml 集成是否阻塞主线程
     - FilterExpr EvalCtx.total_memory 是否每次重算（应缓存）
   - 架构审查：
     - ADR-0022 决策是否完整记录（含 migration path）
     - 历史ADR-0016 / 0013 Status 是否正确改 Superseded / Deprecated
     - trusted_signers.rs 新模块设计是否符合 surgical 原则
   - 安全性审查：
     - trusted_signers.toml 用户配置 regex 是否安全（regex crate DoS 防护）
     - WinVerifyTrust HRESULT 扩展是否有路径泄漏
   - 跨平台审查：
     - 现在 Windows-only，cfg(target_os="windows") 还需要吗（保留更安全）
     - macOS 用户能否降级（不能，ADR-0022 决策）
   - 性能审查：
     - NetworkIn HashSet 改造后性能（benchmark 如有）
     - trusted_signers 加载是否每次 score 调用都读（应在 SecurityScorer::new 时一次）
   - 完整性检查：
     - plan.md 中 v0.12 cycle 阶段 1-5 已 `[x]`
     - tech-debt TD-17/19/23/26/27/28/29/30/32/33/35/36 全标 Fixed
     - CONTEXT.md 术语段 + 演进历史段完整
   - 产出结构化问题清单（P0 / P1 / P2 三档）

2. **修 P0 / P1**（来自 REVIEW-14.md）
   - 逐项修复，每项修复后跑相关测试
   - 单独 commit `fix(v0.12.0): REVIEW-14 P1-N <description>`

3. **归档 P2 到 tech-debt.md**
   - 加 v0.13.0+ 候选段
   - 把 REVIEW-14.md P2 转 TD-38+ 格式

4. **版本号 bump 0.11.0 → 0.12.0**
   - `Cargo.toml` 改 version
   - `cargo build --release` 让 Cargo.lock 同步

5. **CHANGELOG 加 v0.12.0 段**
   - 把 `[Unreleased]` 改为 `## [0.12.0] - YYYY-MM-DD`
   - 加阶段汇总（stage 1 Spike / stage 2 Linux 移除 / stage 3 签名完整度 / stage 4 FilterExpr / stage 5 杂项 / stage 6 收尾）
   - 「已知限制」段：移除 Linux 相关条目；加「Windows-only 平台」

6. **README 更新**
   - banner 加 v0.12.0 段（Windows-only 决策 + UX polish）
   - 平台支持表：删 Linux / macOS 列（stage 2 应已删，本阶段二次确认）
   - FAQ 加 v0.12.0 相关条目（trusted_signers.toml / mem% 修复 / 为什么 Windows-only）

7. **CONTEXT.md 更新**
   - 术语演进历史加 v0.12.0 阶段 6 行
   - v0.11.0 段从「已发布」保持

8. **stage docs 头部加 ✅ 已发布标记**
   - `docs/stages/v0.12-stage-{1..6}.md` 头部加 `> ✅ **已完成**（v0.12.0 阶段 N 会话产出，YYYY-MM-DD）`

9. **tech-debt 终态确认**
   - TD-17 ✅ Fixed（决策不追这个方向）
   - TD-19 ✅ Fixed（同上）
   - TD-23 / 26 / 27 / 28 / 29 / 30 / 32 / 33 / 35 / 36 ✅ Fixed in v0.12.0
   - TD-31（FilterExpr v3 跨 ctx）保持 ⏸（v0.13+ 候选）
   - 新加 TD-38+（REVIEW-14 P2 归档）

10. **打 tag v0.12.0**
    - `git tag -a v0.12.0 -m "v0.12.0：Windows-only 平台定位 + UX polish（trusted_signers + mem% + regex escape + 6 个 P2 修复）"`
    - 不 push，等用户确认

11. **启动验证**（admin only，Windows）
    - `cargo build --release`
    - `./target/release/proc.exe` —— 启动 TUI，确认无 Linux 相关降级提示
    - `./target/release/proc.exe diag` —— JSON 输出含 `dns_collector: etw`（admin）
    - `./target/release/proc.exe ls --filter 'mem > 5%'` —— 验证 mem% 按百分比换算
    - `./target/release/proc.exe flows --filter 'remote_addr =~ /192\.168\/24/'` —— 验证 regex escape

**验收标准**：
- REVIEW-14.md 所有 P0 / P1 标 Fixed
- `cargo test --release -q` ≥ 1130 passed / 0 failed
- Cargo.toml 版本号 0.12.0
- README + CHANGELOG + CONTEXT 完整反映 v0.12.0
- `git tag v0.12.0` 已打（未 push）
- tech-debt 全部相关 TD 标 Fixed；新 P2 归档到 v0.13.0+ 候选段
- Windows admin 实跑 proc 全部新功能正常

**容量预估**：~500 行（REVIEW-14 ~300 + README/CHANGELOG ~200），工具调用 30-50 次。

---

**最终交付物清单**（完工后输出，无下一阶段）：

```
═══════════════════════════════════════════════════
✅ v0.12.0 项目交付完成。

【最终交付物】
- Cargo.toml: 0.12.0
- CHANGELOG.md: [0.12.0] 段完整
- README.md: banner + 平台表（仅 Windows）+ FAQ
- CONTEXT.md: v0.12.0 段完整（含术语演进历史）
- docs/adr/: 0022 Windows-only（Accepted）/ 0016 (Superseded) / 0013 (Deprecated)
- docs/stages/v0.12-stage-{1..6}.md: 全部 ✅ 已发布标记
- docs/reviews/REVIEW-14.md: P0/P1 全 Fixed
- docs/tech-debt.md: TD-17/19/23/26/27/28/29/30/32/33/35/36 ✅ Fixed；新 P2 归档 TD-38+
- git tag v0.12.0: 已打（未 push）

【基线测试】
- cargo test --release -q: N passed / 0 failed / 3 ignored
- cargo fmt --all -- --check: 通过
- cargo clippy --release --all-targets -- -D warnings: 0 warnings
- cargo build --release --no-default-features: 通过

【v0.12.0 cycle 摘要】
- 阶段 1：ADR-0022 + Cargo.toml 清理 + 范围审计（Spike）
- 阶段 2：Linux 代码移除（最大 Slice）
- 阶段 3：签名验证完整度 + trusted_signers.toml
- 阶段 4：FilterExpr 修复（mem% + regex escape）
- 阶段 5：杂项小修（6 个 TD）
- 阶段 6：Review + 收尾 + tag v0.12.0

【下一步候选（v0.13.0+）】
- TD-31 FilterExpr v3 跨 ctx 表达式（大型架构改动）
- REVIEW-14 P2 归档项
- 新方向：per-process bytes_out/in / 关系图可视化 / Windows Service 模式 / 报告导出

请用户确认交付后 push tag v0.12.0：
  git push origin v0.12.0

═══════════════════════════════════════════════════
```
