# v0.20 fixture 录制计划（70 query）

> **状态**：stage 2 已落地基础设施（MockProvider 回放 + record_fixture 工具 +
> 27 个 seed fixture 文件）；**真实 LLM 录制挪 stage 3b 末段**（见下方
> 「stage 2 落地状态」段）。
>
> **关联**：[`v0.20-brainstorm.md`](v0.20-brainstorm.md) 附录 A（70 query 完整清单）+
> 决策 5（JSONL 格式）+ [ADR-0030](../adr/0030-builtin-ai-agent.md) D5。

## 目的

MockProvider fixture 回放让 CI 跑 agent loop **零 LLM 调用**：录制真实 LLM
（Gemma 4 E2B + Anthropic Sonnet 各一组）的响应到 JSONL 文件，测试时按 query
匹配确定性回放。收益：确定性强（同输入同输出）/ 零 API 成本 / 无需 GPU /
CI 不依赖 llama-server。

## fixture 格式（决策 5 拍板 JSONL）

```jsonl
// tests/fixtures/agent/<scenario>-<level>.jsonl，每行一个 JSON 对象
{"query_hash":"a1b2c3...","request":{"messages":[...],"tools":[...]},"response_deltas":[{"Text":"查看"},{"Text":"系统"},{"ToolCall":{"name":"proc_metrics_system","args":{}}},{"ToolResult":{...}},{"Text":"当前 CPU 23%..."},{"EndTurn":{"stop_reason":"end_turn"}}]}
```

字段：

| 字段 | 含义 |
|---|---|
| `query_hash` | query 文本 + provider 名的 SHA-256 前 16 hex（回放匹配键）|
| `request` | 录制时的完整请求快照（messages + tools），diff / debug 用 |
| `response_deltas` | `Delta` 枚举序列（Text / ToolCall / ToolResult / EndTurn），回放时逐条 yield |

## 文件组织（9 场景 × 3 level = 27 文件）

```
tests/fixtures/agent/
├── performance-diagnose-l0.jsonl     # 场景 1 L0
├── performance-diagnose-l1.jsonl     # 场景 1 L1
├── performance-diagnose-l2.jsonl     # 场景 1 L2（录制留档，v0.21 启用）
├── process-diagnose-{l0,l1,l2}.jsonl # 场景 2
├── docker-{l0,l1,l2}.jsonl           # 场景 3
├── usb-{l0,l1,l2}.jsonl              # 场景 4
├── security-{l0,l1,l2}.jsonl         # 场景 5
├── recording-{l0,l1,l2}.jsonl        # 场景 6
├── flow-{l0,l1,l2}.jsonl             # 场景 7
├── monitor-{l0,l1,l2}.jsonl          # 场景 8
├── dns-{l0,l1,l2}.jsonl              # 场景 9
└── README.md                          # 录制说明（stage 2 落地）
```

70 query 清单见 brainstorm 附录 A（10 + 10 + 10 + 10 + 10 + 5 + 5 + 5 + 5）。
Level 分布：L0 23 / L1 27 / L2 20。

## 录制策略

| Provider | 录制内容 | 单 query 耗时 | 总耗时（70 query）|
|---|---|---|---|
| Gemma 4 E2B（`--no-thinks`）| 全部 70 query | ~10s | ~12 分钟 |
| Anthropic Sonnet 4.6 | 全部 70 query（对照）| ~3s | ~3.5 分钟 |

- **录制时机**：stage 2 末段（MockProvider / record_fixture / ToolRegistry /
  AgentRunner 骨架全部落地后）。LLM 响应依赖 system prompt + tool schema 的
  最终形态，提前录制会作废。
- **失败重试**：单 query 最多重试 2 次；重试后仍失败 → 该 query 标记
  `record_failed`，不阻塞其余 query。
- **L1 验收口径**（brainstorm 风险 1 mitigate 4）：L0 23 query 必须全部有可用
  fixture；L1 27 query ≥ 22 个有可用 fixture（80%）；L2 20 query 录制留档
  不验收（v0.21 启用）。

## 录制工具接口（stage 2 落地实装）

```rust
// src/agent/record_fixture.rs
pub struct FixtureRecorder {
    provider: Box<dyn LlmProvider>,
    output_dir: PathBuf,
}

impl FixtureRecorder {
    /// 批量录制：跑 query 清单，每条结果 append 到对应 <scenario>-<level>.jsonl。
    pub async fn record_all(&mut self, queries: &[FixtureQuery]) -> Result<RecordReport>;
}

pub struct FixtureQuery {
    pub scenario: &'static str,   // "performance-diagnose"
    pub level: u8,                // 0 / 1 / 2
    pub query: String,
}

pub struct RecordReport {
    pub recorded: usize,
    pub failed: Vec<String>,
}
```

## MockProvider 回放路径（stage 2 实装）

1. 构造时读 `fixtures_dir` 下全部 `*.jsonl`，建 `query_hash → response_deltas`
   内存索引。
2. `complete()`：取最后一条 user message 计算 query hash，命中 → 按 deltas
   重放拼 `CompleteResponse`；未命中 → `LlmError::Config` 报「fixture 缺失」。
3. `stream()`：同一索引，deltas 逐条 yield。
4. 回归测试：`test_agent_mock_provider_replay_50_queries`（brainstorm 测试矩阵）
   —— L0 23 + L1 27 共 50 query 全部确定性回放。

## stage 2 落地状态（2026-08-15）

实际落地与本计划的两处偏差（`docs/stages/v0.20-stage-2.md` 决策 C / 决策 D）：

1. **真实 LLM 录制挪 stage 3b 末段**：stage 2 只有 MockProvider，无真实
   provider 可录（LlamaCppProvider 在 stage 3a、AnthropicProvider 在 stage 4、
   AgentRunner 的 system prompt 最终形态在 stage 3b）。stage 2 落地
   `record_fixture.rs` 完整录制工具（provider 可注入）+ **seed fixture**
   （70 query 合成响应，结构合法、确定性回放，供 CI 回放测试与格式验证）；
   stage 3b 末段用 LlamaCppProvider 真实录制后逐文件覆盖替换。
2. **行格式加 `query` 字段**：原计划的 `query_hash` 单键需预先算 SHA-256 且
   无法肉眼核对。落地为每行含 `query` 原文（必填）+ `query_hash`（可选，
   加载时现算校验）；hash 输入只含 query 文本（不含 provider 名——录制
   provider 与回放 provider 名不同，含名字会永不命中）。

文件名落地为 `<scenario>-l<level>.jsonl`（`-l0` / `-l1` / `-l2`）；
`monitor-l2.jsonl` 为空文件（场景 8 无 L2 query），保持 27 文件结构完整。
