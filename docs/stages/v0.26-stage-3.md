# 阶段 3：Slice B — 展示层主项（README 评估者视图 + AI/eval 叙事章 + performance.md）

> **独立会话指令**：`阅读 CONTEXT.md 和 docs/stages/v0.26-stage-3.md，完成所有任务后确认完成，并输出下一阶段启动指令包`
>
> **开工前只需阅读**：
> - `CONTEXT.md`（领域词汇表，私有 .gitignore 文件）
> - `docs/stages/v0.26-brainstorm.md`（cycle 总览 + 执行手册 + 打包清单 ①②④ 组 + 风险 1/2/3/8）
> - `docs/stages/v0.26-stage-3.md`（本文件）
> - `docs/adr/0036-presentation-sprint.md`（D1 README 信息架构 + D5 数字溯源纪律 + stage 2 实装注记）
> - `docs/stages/v0.26-stage-1.md` 附录 A / D / E / F（bench 取数 / 竞品核实 / GIF 规格 / 亮点数字——**stage 3 数字唯一来源**）+ 任务 4 README 底图
> - `docs/stages/v0.26-stage-2.md` required checks 段（badge 挂接前提）
> - `README.md` 现文（重构对象，以段为单位 diff 可审）
> - `docs/reviews/REVIEW-v0.22.md` / `REVIEW-v0.23.md` / `REVIEW-v0.24.md`（eval 章数字溯源原文）
>
> **不需要读**：其他 stage doc / 其他 cycle brainstorm / ADR 全集（除 0036）

---

## 目标

实装 v0.26 打包清单组①（README 评估者视图）+ 组②（AI agent + eval 叙事立牌）+ 组④（性能叙事），ADR-0036 D1/D5 全面兑现（brainstorm 决策 2 打包清单）：

1. **README 评估者视图**（D1 四件，插入位置 = `# proc` 标题 + 首段定位之后、release banner 之前）：CI badge 条（**远端 CI 绿前提不成立 → 注释占位**，见「已知阻塞」段）+ demo GIF（附录 E 规格；`docs/assets/demo.gif` 不存在 → `<!-- demo GIF -->` 占位，风险 1 不阻塞）+ mermaid 分层架构图（UI → 面板 → controllers → workers → 采集层 + 横切 security / record-replay / MCP 46 / agent）+ 量化亮点条（附录 F 数字）+ 竞品对比表（附录 D 核实数据 + as-of 标注 + 宁缺毋滥清单）。既有 825 行全部后移不删（风险 8 双受众）。
2. **README「AI Agent 与 eval 纪律」章**：六列 eval 矩阵 + 预登记门槛（净通过差 ≥ +7 且 L2 方向性）+ 负结果三连归档叙事（GBNF 结构性互斥 / prompt v3 带外向下 revert / RAG「机制成立但 E2B 兑现不了」两分）+ L2 full-chain 5% 能力边界——每个数字对回 REVIEW-v0.22/23/24 原文（D5：错误数字比没有更伤）。
3. **docs/performance.md**（新）：附录 A 7 项 bench 25 数据点成文 + v0.13 基线对照 + 归因只写已溯源项（refresh_heavy 2.9× → TD-47 `4c7e294`；其余提升不写归因）+ 启动时间/内存实测补录（本会话实测）。
4. **README `## Benchmark` 段改链**：链到 `docs/performance.md` + 性能摘要段（v0.13 基线 + v0.26 对照两个口径并存）。

**完工后状态**：零 rs 变更（纯 doc 阶段）；回归双档 1744/0/10 + 1768/0/11 不变（80 行）；MCP tool 46 / catalog 47 / runtime deps +0 不变锚在位；README/performance.md 每个数字有溯源（本文件附录核对表，stage 4 Review 抽查 ≥ 10 个）。

---

## 前置依赖

- 阶段 2 Slice A 已 `[x]`（commit `7a62821`，2026-08-31）：R1 根治 + gate.sh 两档 + proptest + proc_smart 口径
- 开工基线（本会话 2026-09-01 实测）：全量回归双档 **1744 / 0 / 10（默认）+ 1768 / 0 / 11（anthropic）**，各 **80 行** test result 核对无截断（R1 已根治，双档首跑全绿）；fmt / clippy 双档 / build 双档 / bench --no-run 全过；git 干净（HEAD `7a62821`，`eval-*-run.log` 本地留存惯例）；Cargo.toml `0.25.0`；MCP tool 46 / agent catalog 47 不变锚在位
- 无 checkpoint 残留（`docs/handoff-*` 不存在）

---

## 依赖测试

- **前一阶段（stage 2）新增/改动测试**（本阶段零 rs 变更不受影响，开工全量回归确认）：
  - `tests/test_filter_proptest.rs`（roundtrip + 乱输入，2 test fn）
  - `tests/test_llama_cpp_provider.rs`（R1 PID 断言重写）
  - `tests/test_mcp_v0_17.rs::test_proc_smart_source_description_in_mod_rs_contains_deprecated`（口径连改后断言）
- 本阶段纯 doc 无新增测试——验收锚是**数字溯源核对清单**（手册步 12 例外条款）

---

## 已知阻塞：远端 CI 全红（本会话开工发现，用户拍板处置 2026-09-01）

**发现**：badge 挂接前提（brainstorm CI 锚「远端 CI 绿」）不成立——本地领先 origin/master **7 个 commit**（v0.25 全 cycle + v0.26 三个 commit 未 push），远端最近一次 run 是 2026-08-27（v0.24-stage-4）且**三 workflow 全红**：

| workflow | 红因（08-27 run 实查） | 修复工作量评估 |
|---|---|---|
| CI `check` | CI 用 stable **1.98.0**（本地 1.95.0），2 处新 lint 红：`src/security/hash_cache.rs:146` `question_mark`（match → `?`）+ `src/smart/mod.rs:186` `manual_filter`（and_then+if → filter） | ~4 行 rs 小修 |
| CI `check-macos` | macOS `--no-default-features` clippy 大量 unused import（Windows-only 代码 cfg-gate 不对称：`inspect/{dlls,env,memory}.rs` / `net_flow/windows.rs` / `security/dll_check.rs` 等） | 中等（cfg 清理 ~10+ 文件） |
| CI `audit` | 2 个 RUSTSEC 漏洞：**rmcp 0.11.0**（RUSTSEC-2026-0189 DNS rebinding，修复需 ≥1.4.0 major 升级）+ crossbeam-epoch 0.9.18（RUSTSEC-2026-0204，patched ≥0.9.20）；另 rustsec/audit-check action 无权限建 check-run（`Resource not accessible by integration`） | rmcp major 升级 = 大工程（0.11→1.4 breaking）；crossbeam 是传递依赖可 `cargo update -p`；action 权限需 workflow `permissions:` 块 |
| Miri | E0433（`windows` crate 找不到）——**历史 8 次全红**（2026-07-01 起），workflow 在非 Windows runner 的编译配置问题 | workflow 修复（小-中） |
| release | 仅 `update-winget` job 红（上游 action `russellbanks/release-automation-winget` 仓库已被删）；**build job 本身绿**（9m44s） | action 替换或 job 移除（小） |

**处置（用户拍板：占位注释 + 记录阻塞）**：
- README badge 以 HTML 注释占位（`<!-- CI badge: ... -->`），注释体内保留完整 badge markdown 语法——CI 转绿后删注释符即启用，零重写
- CI 修复本身**不在 stage 3 范围**（rs / workflow / 依赖变更都违反纯 doc 锚）——修复决策留 stage 4 搭车评估（clippy 2 处小修是否搭车）或 v0.27 cycle
- 亮点条「CI」按附录 F **存在性口径**写（ci.yml + miri.yml + release.yml 三 workflow + miri 覆盖 unsafe UB 检测），**不断言远端绿状态**（D5 诚信纪律）
- demo GIF 同款占位待遇（附录 E：`<!-- demo GIF: docs/assets/demo.gif -->`）

---

## 任务清单

### 1. README 评估者视图插入（~250-300 行，ADR-0036 D1 四件）

**插入位置**：`# proc` 标题 + 首段定位（现 1-3 行）之后、v0.20 release banner（现 5 行）之前——「现有内容后移不删」（底图处置口径）。评估者 10 分钟视图四件顺序（D1 阅读序）：

1. **CI badge 条**：注释占位（CI + release 两个 badge，完整语法留注释体内；URL `https://github.com/Alfroul/proc/actions/workflows/{ci,release}.yml/badge.svg`——本会话已核实 workflow 文件名）
2. **demo GIF**：`<!-- demo GIF: docs/assets/demo.gif（附录 E 规格，用户录制后挂载） -->` 占位
3. **mermaid 分层架构图**：UI（TUI ratatui + CLI clap + MCP client 接入）→ 领域面板（10 主面板）→ App controllers（PanelController ×5 + Inspector/Replay + CommandPalette）→ workers（SnapshotWorker 泛型 + WorkerManager 指数退避重启 5s/30s/5min + Light/Heavy/Smart 周期 worker）→ 采集层（sysinfo / ETW ×3（DNS / Schannel SNI / DiskIO）/ sysinfo delta / smartctl / GetExtendedTcpTable / bollard）+ 横切四件（security 18 项评分 / record-replay 双格式 / MCP server 46 tools / agent providers+registry+RAG+eval）。术语全部对齐 CONTEXT.md 当前术语
4. **量化亮点条 + 竞品对比表**：亮点条用附录 F 数字（65,074 src / 26,968 tests / 1744+1768 双档 / 80 test binaries（stage 2 后口径）/ 46 MCP tools / 47 catalog / 36 ADR / 61 TD / 25 releases）；对比表用附录 D（四竞品 stars / 语言 / 最新 release / AI-MCP 缺位 + as-of 2026-08-31 + btop4win 注脚）+ proc 行只写「proc 有」断言；宁缺毋滥清单三纪律（可写 stars/语言/release/AI-MCP 缺位；谨慎功能矩阵（只写 proc 有）；不写竞品性能数字）

### 2. README「AI Agent 与 eval 纪律」章（~100-150 行，组②）

插入在既有「内置 AI Agent」段（现 276-367 行）**之后**、MCP server 段之前（或并入该段尾部——以段为单位 diff 可审，现有内容不删）。内容四块（数字全部溯源，见附录核对表）：

- **六列 eval 矩阵**：E2B 基线（v0.22）/ promptv2 / best（方差列）/ rag-on / v3（v0.23/24 实验）+ QUICK 冒烟列——净通过 / L0 / L1 / L2 full-chain / output_degraded / 时长六维
- **预登记门槛**：换默认 = 净通过差 ≥ +7 且 L2 方向性改善（REVIEW-v0.24 原文）+ 方差带标尺（±3 通过数 / ±6 失败模式计数，REVIEW-v0.23）
- **负结果三连归档叙事**：① GBNF 结构性互斥（12 请求全部 400 拒绝，绑定 llama-server b8685）② prompt v3 带外向下 revert（净通过 27，-5/-8，TD-60 关闭）③ RAG「机制成立但 E2B 兑现不了」两分（召回 80% / 引用 57% / 干扰 0 / degraded -12 超带 vs 净通过 +2 带内）
- **L2 能力边界**：full-chain 1/20（5%）+ chain-step 12/43（28%）双口径——E2B 底座多步规划能力边界，模型升级是唯一杠杆（REVIEW-v0.24 杠杆穷尽结论）

### 3. docs/performance.md（新，~150-200 行，组④）

- 头部：机器/工具链口径（i7-13700HX / 16GB / Win11 / rustc 1.95.0，与 v0.13 基线同机）+ 方法（criterion 默认参数，分批纪律）+ 数据溯源声明（转写自 stage-1 附录 A = criterion 原始输出，非记忆值）
- 7 项 bench 各一小节：本次 mean vs v0.13 mean 对照表（附录 A 逐行转写）+ 判定行
- 归因段：**只写已溯源项**——refresh_heavy 1000 进程 2.9×（16.47→5.69 ms）→ TD-47 `4c7e294`（v0.17 stage 2 parent_chain `Arc<str>` 重构零 heap alloc）；其余提升（filter_expr ~2× / search ~2× / deserialize 1.9× / tui_draw 1.2-1.5×）**不写归因**（D5 宁缺毋滥：综合因素含工具链全重编译）
- **启动时间/内存实测**（本会话实测，方法与数字如实记录）：CLI 子命令 wall time（`proc ls --limit 5` × 3 次）+ 运行内存（`proc export` 期间 WorkingSet 峰值）——回归跑完后测（避免并发负载污染）
- 总结论：25 数据点全无回归（最差 +7% 同量级）+ 1000 进程规模核心路径全部 < 6 ms

### 4. README `## Benchmark` 段改链（~15 行变更，风险 8）

现 810-822 段改为：一段性能摘要（核心结论 3 行：25 数据点无回归 / refresh_heavy 2.9× 溯源 TD-47 / 1000 进程 hot path < 6 ms）+ 链 `docs/performance.md`（v0.26 实测 + v0.13 基线对照）+ 保留 `cargo bench` 用法与「不在 CI 跑」口径。既有 PERF-BASELINE-v0.13 链接保留。

---

## 附录：数字溯源核对表（D5 验收锚，stage 4 Review 抽查 ≥ 10 个）

**README/performance.md 每个数字的来源对照**（写作时逐个自查打 ✅）：

| # | 数字 | 出现位置 | 来源 |
|---|---|---|---|
| 1 | 65,074 src 行（65K） | 亮点条 | 附录 F（stage-1 实测） |
| 2 | 26,968 tests 行（27K） | 亮点条 | 附录 F |
| 3 | 1744 / 1768 双档回归 | 亮点条 | stage-2 完工记录 + 本会话开工基线（80 行核对） |
| 4 | 80 test binaries | 亮点条 | stage-2 完工记录（79→80，test_filter_proptest）+ 本会话核对 |
| 5 | 46 MCP tools / 47 catalog | 亮点条 + 对比表 | 附录 F（grep `proc_` = 46）+ brainstorm 不变锚 |
| 6 | 36 ADR / 61 TD / 25 releases | 亮点条 | 附录 F（本会话 `git tag \| wc -l` = 25 复核） |
| 7 | 竞品 4 行 stars / release / 语言 / AI-MCP 无 | 对比表 | 附录 D（GitHub API 2026-08-31，as-of 标注） |
| 8 | btop4win Windows 注脚 | 对比表 | 附录 D 附带发现（最后 push 2025-10-12） |
| 9 | E2B 基线 32 净通过 / L0 17/23 / L1 14/27 / L2 1/20 + 12/43 / degraded 21 | eval 章 | REVIEW-v0.22 line 17/29 + REVIEW-v0.23 line 29 |
| 10 | promptv2 33 / best 方差列 35 / ±3±6 方差带 | eval 章矩阵 | REVIEW-v0.23 line 29-30 + 观察 2 |
| 11 | rag-on 37 / L0 20/23 / L1 17/27 / degraded 19→9 / 召回 80% / 引用 57% / 干扰 0 | eval 章 | REVIEW-v0.24 line 29-33 + line 98 |
| 12 | v3 27 / -5/-8 带外 / degraded 24 / TD-60 关闭 | eval 章 | REVIEW-v0.24 line 33 |
| 13 | GBNF 12 请求 400 拒绝 + 错误体原文 | eval 章 | REVIEW-v0.23 line 28 |
| 14 | QUICK 26 条（18m46s，L0 7/9 / L1 4/9 / L2 0/8 + 4/17） | eval 章矩阵 | REVIEW-v0.22 line 28 |
| 15 | 预登记门槛 净通过差 ≥ +7 且 L2 方向性 | eval 章 | REVIEW-v0.24 line 100/124 |
| 16 | 47m19s / 37m07s / 38m43s / 25m20s / 54m53s / 18m46s 各列时长 | eval 章矩阵 | REVIEW-v0.22/23/24 对应行 |
| 17 | 7 项 bench 25 数据点 + v0.13 对照 | performance.md | 附录 A（criterion 输出转写） |
| 18 | refresh_heavy 2.9×（16.47→5.69 ms）→ TD-47 `4c7e294` | performance.md + README 摘要 | 附录 A + 归因段（v0.17 stage 2） |
| 19 | 启动时间 / 内存 | performance.md | 本会话实测（方法注记） |
| 20 | CI 三 workflow（ci/miri/release） | 亮点条（存在性口径） | 附录 F + 本会话 `ls .github/workflows/` 复核 |

**写作纪律**（附录 D 宁缺毋滥 + D5）：
- 不写：竞品性能数字 / 竞品功能矩阵「没有」断言（除已核实的 AI-MCP 缺位）/ 远端 CI 绿状态 / 未溯源归因
- 约数写法允许（65K/27K），精确值以附录 F 为准

---

## 验证

- **回归双档不变**：1744 / 0 / 10 + 1768 / 0 / 11（80 行核对）——零 rs 变则零重编译（`cargo test --release -q` 直出）
- **不变锚**：MCP tool 46（`grep 'name = "proc_' src/mcp/handler/mod.rs \| wc -l`）/ catalog 47 / runtime deps +0
- **数字溯源自查**：附录核对表 20 项逐个 ✅（本文件验收时回填状态列）
- **mermaid 渲染**：本地核验语法（mermaid CLI 不可用则人工逐行核 + GitHub 渲染待 push 后核）
- **链接有效**：README/performance.md 内全部相对链接本地存在性核验
- **badge/GIF 占位**：注释体不渲染（README 预览不受影响）

---

## 完工记录（执行会话回填，2026-09-01）

- **README 变更规模**：+114 / -1（`git diff --stat`，Benchmark 段重写占 1 删）；既有 825 行全部后移不删（`## 功能` / `## 安装` / `## 快捷键` / `## 命令行` / `## 平台支持` / `## FAQ` / `## Benchmark` / `## License` 段标题全保留核验）——风险 8 兑现
- **评估者视图四件落地**：badge 注释占位（注释体含 CI + release 完整 badge 语法，CI 转绿删注释符即启用）+ demo GIF 注释占位（附录 E 规格注记）+ mermaid 分层架构图（UI → 领域面板 → controllers → workers → 采集层 + 横切四件；术语逐个对齐 CONTEXT.md；「23 顶层子命令」为本会话 `--help` 实测口径，替代 README 旧文 v0.15 时代的「32 子命令」过时数）+ 亮点条（3×3 表）+ 竞品对比表（附录 D 四竞品 + as-of 2026-08-31 + 「无」判定方法注记 + btop4win 注脚 + proc stars 如实 1）
- **eval 章落地**：六列矩阵（基线 / QUICK / promptv2 / best 方差 / rag-on / v3）+ 判读纪律五条（方差带 ±3±6 / 预登记门槛 ≥+7 且 L2 方向性 / 负结果三连 / L2 边界 / 杠杆穷尽）；「—」格注明「REVIEW 未单列该维度」不自创；rag 列 degraded 19→9 按归档四列表（vs 方差列 -10 / vs 基线 -12 双口径在负结果三连段注明）
- **performance.md 落地**（新 ~120 行）：口径声明（转写自附录 A 非记忆值 + 同机声明）+ 6 节 bench 对照 + 归因只写 TD-47 `4c7e294`（其余提升明确「不写归因」）+ 启动/内存实测（`proc ls --limit 5` ×3 = 2291/2527/2875 ms 合并口径注记 + record headless 8s 采样峰值 56.7 MB + 单机单次样本非基准声明）
- **Benchmark 段改链**：性能摘要三点（25 数据点无回归 / 2.9× 溯源 / <6ms）+ 链 performance.md；PERF-BASELINE-v0.13 链接与 `cargo bench` 用法保留
- **CI 阻塞处置**：badge 占位注释；修复清单在上方「已知阻塞」段（clippy 2 处 / macOS unused imports / rmcp+crossbeam 漏洞 / miri / winget / 7 commits 未 push），修复留 stage 4 搭车评估或 v0.27
- **数字溯源 20 项核对**：全部 ✅（亮点条 8 数字本会话复核：tag 25 / ADR 36 / TD 61 / tool 46 / 80 行 / stars 1；eval 数字逐项对回 REVIEW-v0.22/23/24 + rag 归档原文）
- **回归双档终值**：**1744 / 0 / 10 + 1768 / 0 / 11**（各 80 行核对）——开工即终值（本会话零 rs 变更，纯 md/doc 改动）；fmt / clippy 双档 / build 双档（no-default-features ± anthropic，9m19s+9m30s 独立 profile 编译）/ bench --no-run全过；git HEAD `7a62821`

---

## 完工收尾（按手册步 16-23）

- [ ] brainstorm 4 stage 总览表勾阶段 3 `[x]`（唯一勾选点）
- [ ] CHANGELOG `[Unreleased]` 加 stage 3 条目
- [ ] ADR-0036「stage 3 实装注记」段回填（README 重构段清单 / performance.md 落地 / GIF 挂载状态 / badge 占位与 CI 阻塞）
- [ ] CONTEXT.md「v0.26.0 新增术语」更新（评估者视图实装词条 / performance.md）
- [ ] commit（模板：`docs(v0.26-stage-3): Slice B 展示层——README 评估者视图（badge 占位+GIF 占位+mermaid 架构图+亮点条+竞品对比表）+ AI Agent 与 eval 纪律章（六列矩阵+门槛+负结果三连）+ docs/performance.md（7 项 bench+启动/内存实测）+ Benchmark 段改链；远端 CI 红阻塞记录（badge 占位处置）`）
- [ ] 输出阶段完成报告 + 阶段 4 启动指令包
