### 阶段 7：Review + 修 P0/P1 + 定稿 + tag v0.9.0

> **独立会话指令**：`阅读 CONTEXT.md、docs/stages/v0.9-stage-7.md、docs/reviews/REVIEW-10.md（本阶段产出），修复 P0/P1 问题并完成 v0.9.0 交付`

**目标**：消费 v0.9.0 阶段 1-6 的全局 Review，修 P0/P1，完成 v0.9.0 交付。

**前置依赖**：v0.9.0 阶段 1-6 全部 `[x]`。

**任务清单**（预测，参考 v0.8.0 阶段 4 同款流程）：

1. **全局 Review（产出 REVIEW-10.md）**
   - 代码质量：JA4 算法正确性（4 个已知 ClientHello hash 是否全对）+ bytes 聚合准确性
   - 安全性：JA4 黑名单机制是否泄漏用户隐私（黑名单上传 / 本地匹配）
   - 性能：kprobe on `tcp_sendmsg` 每 packet 都触发，是否影响网络吞吐
   - 跨平台：BoringSSL / LibreSSL 路径是否真测过（v0.9 阶段 5 如未真测，标 TD）

2. **修 P0/P1**
   - 逐项 commit + 测试
   - REVIEW-10.md 标 Fixed

3. **版本号 bump 0.8.0 → 0.9.0**
   - Cargo.toml + Cargo.lock
   - CHANGELOG 加 v0.9.0 段（阶段 1-7 列变更）
   - README banner 加 v0.9.0 主题（eBPF SNI / JA4 / bytes 端到端）
   - CONTEXT.md 术语演进历史加 v0.9.0 行

4. **release CI 验证**
   - 看 v0.8.0 release CI 是否 ebpf build 成功
   - 如成功：v0.9.0 release CI 应同样跑通
   - 如失败：v0.9 阶段 1 修后再跑

5. **打 tag v0.9.0**
   - `git tag -a v0.9.0 -m "v0.9.0：eBPF TLS SNI + JA4 指纹 + bytes 累计"`

6. **启动验证**
   - 全套命令在 Linux 真实环境跑通
   - curl / chromium / firefox 各触发一次 → proc flows 显示 3 个 flow 各有 SNI + JA4 + bytes

**验收标准**：
- REVIEW-10.md P0/P1 全 Fixed
- `cargo test --release -q` ≥ 950 passed / 0 failed
- `cargo build --release --features ebpf` 通过（Linux）
- Cargo.toml 0.9.0
- README + CHANGELOG 完整反映 v0.9.0
- 端到端：3 种客户端（curl / chromium / firefox）触发 flow，proc flows 显示完整 SNI/JA4/bytes
- `git tag v0.9.0` 已打

**容量预估**：~600-1000 行（README/CHANGELOG 400 + P0/P1 修复 200-600），工具调用 40-60 次。
