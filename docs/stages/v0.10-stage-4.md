### 阶段 4：Review + 修 P0/P1 + 定稿 + tag v0.10.0

> ✅ **已发布**（v0.10.0，2026-06-28；本次会话产出）
>
> **独立会话指令**：`阅读 CONTEXT.md、docs/stages/v0.10-stage-4.md、docs/reviews/REVIEW-11.md（本阶段产出），修复 P0/P1 问题并完成 v0.10.0 交付`

**目标**：消费 v0.10.0 阶段 1-3 全局 Review，修 P0/P1，完成 v0.10.0 交付。**这是 v0.8-v0.10 三部曲最后一站**——完成后 proc 的网络流量分析能力 Linux + Windows 双全。

**前置依赖**：v0.10.0 阶段 1-3 全部 `[x]`。

**任务清单**（预测）：

1. **全局 Review（产出 REVIEW-11.md）**
   - 跨平台一致性：Linux ebpf flow 与 Windows Schannel flow 在 UI 显示是否一致
   - 安全性：Schannel ETW 是否在 worker Drop 时正确 stop（不留孤儿 session）
   - 性能：Schannel callback 频率是否影响 TUI 主线程（看 worker_metrics）
   - 兼容性：Win10 1809 之前版本降级路径是否优雅

2. **修 P0/P1**

3. **版本号 bump 0.9.0 → 0.10.0**
   - Cargo.toml + Cargo.lock
   - CHANGELOG 加 v0.10.0 段
   - README banner 加 v0.10.0 主题（Windows Schannel SNI 落地，跨平台对齐）
   - CONTEXT.md 术语演进历史加 v0.10.0 行
   - 平台支持表：Windows 行加「SNI / TLS 流量分析」✓

4. **打 tag v0.10.0**
   - `git tag -a v0.10.0 -m "v0.10.0：Windows Schannel ETW SNI 落地，跨平台流量分析对齐"`

5. **启动验证**
   - Windows admin：cargo build / cargo test / proc / proc flows / curl https://example.com 验证
   - Linux（如可得）：v0.9 路径回归验证
   - 跨平台录屏 round-trip：Windows 录 .prec → Linux replay → ProcessFlow.source 字段正确

**验收标准**：
- REVIEW-11.md P0/P1 全 Fixed
- `cargo test --release -q` ≥ 970 passed（v0.9 的 950 + v0.10 加 ~20）
- Windows admin 跑 proc flows 显示 SNI
- Cargo.toml 0.10.0
- README + CHANGELOG + CONTEXT 完整反映 v0.10.0
- `git tag v0.10.0` 已打
- TD-18 标 Fixed
- v0.8-v0.10 全部 tech-debt（TD-12/13/15/16/17/18/19）全 Fixed

**容量预估**：~500-800 行（README/CHANGELOG 300 + P0/P1 修复 200-500），工具调用 30-50 次。

---

**v0.10.0 完工后**：v0.8-v0.10 三部曲收官，proc 网络流量分析能力对齐 Linux + Windows 双平台。下一步候选（v0.11.0+）：
- Linux 用户 rootless ebpf（用 libbpf-rs 替代 aya 探索）
- Windows 11 ETW TLS keylog（需 Schannel IRP hook，深度大）
- FilterExpr v2：支持 ALPN / JA4 / bytes 字段查询
- worker restart 实现补齐（v0.6 TD-4 长期挂账）
