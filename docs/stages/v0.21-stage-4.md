### 阶段 4：Review + 收尾 — REVIEW-v0.21 + CHANGELOG + README TUI Agent 章节 + Cargo bump 0.21.0 + tag

> ✅ **已完成**（2026-08-19，基线双档全量回归 1607/0/8 + 1631/0/9 无回归 + fmt / clippy 双档 / build 双档 / bench --no-run 全过；REVIEW-v0.21（P0 0 / P1 0 / TD-58 归档）+ TD-58 入 tech-debt + CHANGELOG [0.21.0] + README TUI Agent 章节 + Cargo 0.21.0 + brainstorm cycle 总结 + tag `v0.21.0`）

> **独立会话指令**：`阅读 CONTEXT.md 和 docs/stages/v0.21-stage-4.md，完成所有任务后确认完成，并输出 cycle 完结报告（含 v0.22 C-cycle 预拍板回顾）`
>
> **开工前只需阅读**：
> - `CONTEXT.md`（领域词汇表，私有 .gitignore 文件）— 重点 v0.21.0 术语段（AgentSession / SessionEvent / ConfirmRequest / ConfirmDecision / run_streaming / AgentPanel / builder 构造链 词条）+ 「v0.21.0 落地变更」段阶段 1~3 行
> - `docs/stages/v0.21-brainstorm.md`（cycle 总览 + 8 决策 + 已知风险 1~6 + 执行手册 + v0.22 C-cycle 预拍板段）
> - `docs/adr/0031-tui-agent-panel.md`（D1~D6 + stage 1/2/3 落地注记）
> - `docs/adr/0030-builtin-ai-agent.md`（v0.20 基座）
> - `docs/reviews/REVIEW-v0.20.md`（四维度 Review 模板参考）
> - `docs/stages/v0.21-stage-3.md`（前一阶段完成报告——stage 3 实测观察归档素材）
>
> **不需要读**：其他 stage doc / 其他 cycle brainstorm / 其他 ADR

---

## 目标

v0.21 cycle 的**Review + 收尾合并段**（brainstorm 决策 5：Slice ≤ 2 时 Review 与收尾合并，末段自含验收 + 修复，~350 行）：

1. **REVIEW-v0.21**（`docs/reviews/REVIEW-v0.21.md` 新）：参考 REVIEW-v0.20 四维度模板（代码质量 / 架构 / 性能 / 完整性——按 stage 1~3 分节），含 stage 3 实测观察归档（全局键踩坑 / llama-server 自愈 / E2B 写操作发现链边界）+ TD 候选评估（含 stage 1 归档的 test_alert flaky 候选）+ cycle 数据汇总 + v0.22+ 候选方向
2. **CHANGELOG**：`[Unreleased]` → `[0.21.0] - 2026-08-19` 正式段（cycle 完结总述 + 关键数字表，参考 v0.20.0 段格式）
3. **README**：TUI Agent 章节（内置 AI Agent 章节扩 TUI 面板路径——palette 入口已定稿 / streaming / y·n confirm / 多轮 / Esc 中断 / Ctrl+D 退出清理）
4. **Cargo bump**：`Cargo.toml` 0.20.0 → 0.21.0（Cargo.lock 随 build 同步）
5. **brainstorm 收尾**：4 stage 总览表 stage 4 勾 `[x]`（唯一勾选点）+ cycle 总结段（对比 v0.20：4 stage / ~2900 行 / 演示能力闭环）
6. **tag `v0.21.0`** + 收尾 commit

**完成后状态**：tag v0.21.0 落地，v0.21 cycle 完结，可开 v0.22 brainstorm（C-cycle 预拍板已记录在案）。

---

## 前置依赖

- v0.21 stage 1 ✅（commit `ae927c3`）+ stage 2 ✅（commit `c766da1`）+ stage 3 ✅（commit `723a30a`，brainstorm 4 stage 总览表已勾选）
- 全量回归 1607 passed / 0 failed / 8 ignored（默认档）+ 1631 / 0 / 9（anthropic 档）
- fmt / clippy（双档）/ build 双档 / bench --no-run 全过；git 工作树干净（HEAD `723a30a`）
- Cargo.toml version 仍为 `0.20.0`（本阶段 bump）

## 依赖测试

- 无新增代码——本阶段纯 doc + 版本收尾，基线三件套 + 双档全量回归确认无回归即可

---

## 任务清单

### 1. 基线验证（开工 8 步的回归 + 三件套）

- 双档全量回归 summary（期望 1607 / 0 / 8 默认 + 1631 / 0 / 9 anthropic）
- fmt / clippy 双档 / build 双档 / bench --no-run；`git status` 干净 + HEAD `723a30a`

### 2. `docs/reviews/REVIEW-v0.21.md`（新 ~200 行）

- 头部：cycle 范围 + Review 范围（4 stage）+ 基线数字（1533/1557 v0.20.0 → 1607/1631 v0.21.0）+ 概览（cycle 定位 + 核心交付）
- 分节四维度审查（参考 REVIEW-v0.20 结构按 stage 分节）：
  - **stage 1 Spike**（ADR-0031 + 类型骨架 + palette 入口 fallback——`A` 键冲突实测与预授权 fallback 决策质量）
  - **stage 2 Slice A**（AgentSession + run_streaming + confirm 协议层——线程/通道桥接架构、ConfirmHook 签名简化、E2B 流式冒烟闭环风险 3）
  - **stage 3 Slice B**（controller + 渲染 + App 集成——apply_event 纯状态迁移可测性、teardown 生命周期、全局键豁免修复）
- **stage 3 实测观察归档**（3 条）：① 全局键踩坑（Agent 模式必须豁免数字/R/D/A 全局捕获——含数字 query 会被切走面板）② llama-server 自愈（中途连接失败 → Error 提示 + provider 惰性重 spawn 继续）③ E2B 写操作发现链边界（写 tool 不在 entry 集内，模型不主动 proc_help 发现时倾向文字解释——确认框触发依赖两层架构发现路径，属模型能力边界非面板缺陷）
- **Findings 表 + TD 候选评估**：test_alert flaky 候选（stage 1 开工首跑 `test_metric_extract_process_cpu` 并发 1 failed，重跑全过——评估是否归档 TD）；TD-55~57 仍 open（视 key）；评估本 cycle 是否有新 TD
- **cycle 数据汇总表**（commits / 回归增量 +74（15+35+23+1 修复后首编译 0）/ ADR / deps +0 / MCP 46 不变）
- **v0.22+ 候选方向段**：引用 brainstorm v0.22 C-cycle 预拍板记录（Eval + Observability / 70 query 泛化 harness / TUI session 观测素材）

### 3. CHANGELOG `[0.21.0]` 正式段

- `## [Unreleased]` 段整体转正：`## [0.21.0] - 2026-08-19`，头部加 cycle 完结总述（对比 v0.20 段格式：cycle 定位 + 四项交付 + 关键数字表 1533→1607 等）
- 保留 stage 3/2/1 子段不动（历史记录）；空 `[Unreleased]` 段不留（下个 cycle 开新）

### 4. README TUI Agent 章节

- 「内置 AI Agent（proc → LLM）」章节扩 TUI 面板路径（sup 标签加 `扩 v0.21.0`）：palette 入口（Ctrl+P 搜「AI Agent」）+ streaming 逐字流出 + tool 步骤行 + 写操作 y/n 确认框（summary 强制展示）+ 多轮追问 + Esc 中断 + Ctrl+D 退出（llama-server 随退清理）+ 会话生命周期（进面板建 session / 惰性 spawn / 退面板 Drop kill）
- 措辞与 brainstorm「快速开始（v0.21 cycle 后）」段对齐（palette 唯一入口已定稿，不提 A 键）

### 5. Cargo bump + brainstorm 勾选 + cycle 总结

- `Cargo.toml` version 0.20.0 → 0.21.0；`cargo build --release` 同步 Cargo.lock
- brainstorm 4 stage 总览表 stage 4 行 `[x]` ✅（唯一勾选点）
- brainstorm 加 cycle 总结段（对比表：4 stage vs 5 stage / ~2900 行 vs ~3000 行 / +74 测试 vs +90 / deps +0 / 演示能力闭环——「现场演示杀手锏」从 CLI 单轮升级为 TUI 面板流式 + confirm）

### 6. 收尾 commit + tag

- commit：`docs(v0.21-stage-4): REVIEW-v0.21 + CHANGELOG 0.21.0 + README TUI Agent 章节 + Cargo bump + tag v0.21.0`（模板见下）
- `git tag v0.21.0`（annotated 或 lightweight 与 v0.20.0 一致——查 `git tag -l -n1 v0.20.0` 确认）
- 输出 cycle 完结报告（含 v0.22 C-cycle 预拍板回顾）

---

## 验收标准

| # | 标准 | 验证方式 |
|---|---|---|
| 1 | 双档全量回归 1607 / 0 / 8 + 1631 / 0 / 9 无回归 | 开工基线验证 |
| 2 | REVIEW-v0.21 四维度齐全 + 实测观察 3 条归档 + TD 候选评估（含 test_alert flaky）| 文档走查 |
| 3 | CHANGELOG [0.21.0] 正式段 + 关键数字表 | 文档走查 |
| 4 | README TUI Agent 章节与实际键位/入口一致（palette / y·n / Esc / Ctrl+D）| 与 help_panel / controller 代码对照 |
| 5 | Cargo.toml 0.21.0 + Cargo.lock 同步 | `cargo build --release` exit 0 |
| 6 | brainstorm stage 4 `[x]` + cycle 总结段 | 文档走查 |
| 7 | tag v0.21.0 + git 干净 | `git tag -l` + `git status` |

---

## Commit message 模板

```
docs(v0.21-stage-4): REVIEW-v0.21 + CHANGELOG 0.21.0 + README TUI Agent 章节 + Cargo bump + tag v0.21.0

v0.21 cycle stage 4 Review+收尾（TUI AgentPanel + streaming + confirm 通道 cycle 完结）：
- docs/reviews/REVIEW-v0.21.md（新）：4 stage 四维度审查 + stage 3 实测观察归档
  （全局键豁免 / llama-server 自愈 / E2B 写操作发现链边界）+ TD 候选评估 + cycle 数据
- CHANGELOG.md：[Unreleased] → [0.21.0] - 2026-08-19 正式段 + cycle 完结总述
- README.md：内置 AI Agent 章节扩 TUI 面板路径（palette 入口 / streaming / y·n
  confirm / 多轮 / Esc 中断 / Ctrl+D 退出清理）
- Cargo.toml：0.20.0 → 0.21.0（Cargo.lock 同步）
- docs/stages/v0.21-brainstorm.md：stage 4 ✅ + cycle 总结段

关键数字：
- 全量回归：1533 → 1607 passed / 0 failed / 8 ignored（默认档，+74）；
  1557 → 1631（anthropic 档，+74）
- 4 stage（1 Spike + 2 Slice + Review+收尾）全交付；ADR-0031（D1~D6）
- Cargo deps +0；MCP tool 46 不变；agent catalog 47 不变

详见 docs/reviews/REVIEW-v0.21.md + docs/stages/v0.21-brainstorm.md。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

---

## 已知风险（本阶段无代码改动，风险均为流程性）

### 风险 1：Cargo bump 后 Cargo.lock 漏同步

`cargo build --release` 会自动更新 Cargo.lock 的 proc 自身 version；commit 前确认 lock 文件已含 `name = "proc"` version `0.21.0`。

### 风险 2：tag 格式与历史不一致

先查 `git tag -l -n1` 确认 v0.20.0 是 annotated 还是 lightweight，保持同款（v0.19.0 / v0.20.0 均已存在的格式）。

### 风险 3：README 键位描述与代码漂移

README 措辞以 `src/view_models/agent_panel_controller.rs` 实装键位 + `src/tui/help_panel.rs` AI Agent 区段为准（Enter 发送 / Esc 中断·退出 / y·n 确认 / PgUp PgDn 滚动 / Ctrl+D 退出），不凭记忆写。

---

## 完工后输出

1. **cycle 完结报告**（4 stage 交付清单 + 关键数字 + Findings 汇总）
2. **v0.22 C-cycle 预拍板回顾**（brainstorm 末段已有记录：Eval + Observability / 70 query 泛化 harness / TUI session 观测素材 / TD-55~57 视 key / L2 系统验收 + 更强模型复测）
3. **git tag `v0.21.0`**
