# 阶段 2：Slice — 修复主项落地（ci.yml 六组 + msrv B + audit 三修 + miri/winget 移除 + 搭车）

> **独立会话指令**：`阅读 CONTEXT.md 和 docs/stages/v0.27-brainstorm.md（重点：打包清单六组表 + 3 stage 总览 stage 2 行 + 已知风险段）+ docs/adr/0037-ci-recovery.md（D1~D5 决策集——stage 2 实装依据）+ docs/stages/v0.27-stage-1.md（取证附录 A-F，实装时引用不自创数字），按手册「开工前必做 8 步」验证基线（回归双档 1744/0/10 + 1768/0/11 各 80 行 + 三件套；首跑红按 TD-62/R1 口径单跑确认后复跑）后，先产出本文件再执行`
>
> **开工前只需阅读**：
> - `CONTEXT.md`（领域词汇表，私有 .gitignore 文件）
> - `docs/stages/v0.27-brainstorm.md`（cycle 总览 + 5 决策 + 执行手册 + 验证矩阵）
> - `docs/stages/v0.27-stage-1.md`（取证附录 A-F）
> - `docs/adr/0037-ci-recovery.md`（D1~D5 决策集——本阶段实装依据）
>
> **不需要读**：其他 stage doc / REVIEW 全集（stage 3 才需要）

---

## 目标

ADR-0037 D1~D5 五组决策全部实装：① check job 测试策略（choke-point env-gate + ETW 两文件 per-test gate + timeout 兜底）② msrv 双修（windows runner + rust-version 1.88 **B 方案**——用户开工指令确认默认 B）③ audit 三修（permissions 块 + crossbeam-epoch update + RUSTSEC-2026-0189 ignore）④ miri.yml 删除 + release.yml update-winget job 删除 + unsafe-audit 措辞同步 ⑤ 搭车（ADR-0003 幽灵引用改代码层锚 + TD-63 新立 + TD 台账 v0.27 段）。**远端验证（push → 三 workflow 观察）属 stage 3，push 为用户拍板项**。

**Slice 完成后状态**：
- 本地全量回归双档与开工基线一致（env-gate 仅 `CI=true` 生效，本地不设——本会话实测 `CI` 环境变量为空，行为零变化的前提成立）
- lockfile 变更（crossbeam-epoch ≥0.9.20）+ Cargo.toml `rust-version` 1.85→1.88 后双档重跑验证
- `cargo +1.88.0 check --no-default-features --all-targets` 本地实证（B 方案依据）

---

## 前置依赖

- stage 1 已 `[x]`（commit `6abdb85`，ADR-0037 + 附录 A-F 在库）
- 开工基线（本会话 2026-09-03 实测）：
  - **默认档 1744 / 0 / 10（80 行核对）**——首跑红在 `test_agent_v0_21_stage_2::test_session_drop_during_confirm_does_not_hang`（**TD-62 本体**，断言「cancel 后 run 应 Interrupted 收尾」），单跑该 binary 36/36 绿（35 passed + 1 ignored）后复跑全档绿——TD-62「首跑红、单跑绿、复跑绿」口径归档，非回归
  - **anthropic 档 1768 / 0 / 11（80 行核对）**——首跑红在 `test_inspect::inspect_aggregates_three_buckets`（「dlls empty」断言，32/80 行处），单跑该 binary 12/12 绿后复跑全档绿——同族「首跑红、单跑绿、复跑绿」负载敏感 flaky（树未变，非回归；与 TD-62 / R1 同家族，本阶段不修留档）
  - 三件套：fmt / clippy 双档 / build 双档 / bench --no-run 全过【实测见完工验证段】
  - 本地 `CI` 环境变量为空（choke-point gate 本地零激活的设计前提实证）
  - git 干净（HEAD `6abdb85`，eval log 本地留存惯例）；无 checkpoint 残留
  - Cargo.toml `0.26.0` ✓（cycle 版本号检查——stage 3 才 bump 0.27.0）
- msrv 处置拍板：**B 方案**（用户开工指令明示默认 B；A 方案 pin 清单留档 stage-1 附录 C.3 备选）

## 依赖测试

- 全量回归 summary 双档（lockfile 变更后完工重跑——brainstorm 验证矩阵要求）
- `cargo +1.88.0 check --no-default-features`（msrv B 本地实证，与 ci.yml msrv job 修正后步骤同口径——`--all-targets` 变体见任务 2 实施注记）
- 不变锚：MCP tool 46 / agent catalog 47 / runtime deps +0

---

## 任务清单

### 1. check job 修复（D1：choke-point gate + ETW gate + timeout）

**choke-point 单点 gate**（主修复）：`src/security/signature.rs::verify_signature_with_policy`（:177）在 mock 短路（`policy_override = Some` 状态机测试不受影响）之后、`is_elevated()` 检查之前加 `CI=true → return SignatureStatus::Unknown`——与本地非提权行为一致（附录 B.1），单点覆盖 lib `real_path_does_not_panic`（:443，实证挂死）+ `test_signature.rs:497` + `SecurityScorer::score()` 全部间接路径（scorer big_request 200 synthetic exe 实证）。

**ETW/真实网络 per-test gate**（附录 B.2）：
- `tests/test_dns_etw.rs`：`ci_env()` helper + 3 个真实路径测试早退（`detect_collector_returns_aligned_tuple` / `detect_collector_windows_round_trip_clean` / `etw_collects_example_com_query_when_admin`）——纯契约测试（as_str / Display / serde / eq）保留 CI 覆盖
- `tests/test_schannel_etw.rs`：同款 helper + 2 个（`worker_spawns_and_drops_cleanly` / `spawn_collects_self_sni_when_admin`）——`sni_record_shape` 保留

**timeout 兜底**：check job `timeout-minutes: 60` + test 步 `timeout-minutes: 45`（D1.3：build ~10min + test 预留 45min + 余量；未知挂点从 6h 挂死止损为确定性失败）。

**test 步跑全量不裁剪**（D1.4）：`cargo test --release` 步骤本身不变（与本地回归同口径；gate 快档仍是本地自愿防线）。

### 2. msrv 双修落地（D2，B 方案）

1. `rustup toolchain install 1.88.0 --profile minimal` → `cargo +1.88.0 check --no-default-features` 本地实证编译通过（先实证后改声明；**实施注记**：原计划的 `--all-targets` 变体实测在任何工具链（1.95 / 1.88 均验证）编不过测试目标——`test_agent_v0_20_stage_2` / `_stage_3b` 引用 `mock-provider` / `llama-cpp` 门控模块（default feature），该失败预存在但 ubuntu 时代死于依赖解析从未暴露（ADR-0037 D2 stage 2 实装注记同步）；msrv job 步骤相应修正为 `cargo check --no-default-features`（lib+bins，与 check job build 步同编译域））
2. `Cargo.toml:5` `rust-version = "1.85"` → `"1.88"`（lockfile 16 包要求 1.86-1.88——1.85 声明已失真；A 方案 pin 4 条链 16 包留档附录 C 备选）
3. ci.yml msrv job：`runs-on: ubuntu-latest` → `windows-latest`（附录 D：ubuntu 上 lib 编译不过，该 job 在 ubuntu 从未可能绿过）+ `dtolnay/rust-toolchain@1.85.0` → `@1.88.0`；步骤不变
4. **实施中发现（D2 连带处置）**：rust-version 1.88 解锁 clippy **let-chain 版 `collapsible_if`**（折叠建议需 MSRV ≥1.88 才启用）——37 处既有代码（16 文件，agent/mcp/replay 等本 cycle 未触碰路径）在本地 1.95 被打红（远端 1.98 同理会红）。处置：`Cargo.toml` 加 `[lints.clippy] collapsible_if = "allow"`（specific allow 实证胜过 generic `-D warnings`）+ tech-debt v0.27 段归档 37 处 let-chain 现代化为 v0.28+ 候选——CI 修复专项不动 16 文件业务代码（手册规则 11 的 >5 处大改边界）

### 3. check-macos job 移除（D3）

ci.yml 删除 check-macos job（:37-54）。依据：附录 D 实证否决 B 路径（74 错误中 ~71 个与 feature 无关，feature 卫生检查在 macOS 从未成立）；`--no-default-features` 卫生已由 check job 的 Windows build 步覆盖；job 11/11 从未绿。ADR-0037 D3 注记在案（CI 矩阵无 macOS 的答辩口径）。

### 4. audit 三修（D4）

1. ci.yml audit job 加 `permissions:` 块（`contents: read` + `checks: write`——rustsec/audit-check action 建 check-run 所需，现状 `Resource not accessible by integration`）
2. `cargo update -p crossbeam-epoch`（0.9.18 → ≥0.9.20，RUSTSEC-2026-0204 patched；纯 lockfile 变更非直接依赖）
3. audit job 加 `ignore: RUSTSEC-2026-0189`（action.yml 实证支持逗号分隔 advisory ID 输入）——rmcp 0.11 SSE Host-header，stdio 默认不受影响（advisory 原文）+ SSE 三条件窄暴露（附录 E），TD-63 追踪 major
4. 本地 `cargo audit` 预验：首次 DB 拉取失败（网络），复验成功——**`cargo audit --ignore RUSTSEC-2026-0189` exit 0**（CI job 同款参数；唯一 vulnerability 即 rmcp（ignore 正对应）、crossbeam-2026-0204 已随 0.9.20 清除、余 6 个 warning 级（unmaintained/unsound/yanked——bincode/paste/anyhow/lru/chacha20 等，不挡 job）；DB 2026-09-03 当日拉取）

### 5. miri / winget 移除 + 措辞同步（D5）

1. 删除 `.github/workflows/miri.yml`（声明性降级——附录 F.1 三重论据：平台不可修 / NT FFI 层 miri 不支持 / 15 处 sleep 时序断言不适合解释器）
2. `release.yml` 删除 update-winget job（:55-65——上游 action 删库 404 + winget-pkgs 无 manifest，分发从未存在，删除零用户破坏）
3. `docs/unsafe-audit.md` 两处措辞：「workflow 待修」→「已移除（ADR-0037 D5）」（§3 miri bullet + §5 结论 3）
4. **README 口径同步（D4/D5 直接推论）**：
   - 亮点条质量门禁列删「miri（unsafe UB 检测）」（:21）
   - 安装段删「方式 3：Windows 包管理器」winget + scoop 两行（:659-661）+ :674 补全部署句 + :774 安装渠道列表——**stage-1 实证 winget-pkgs 无 manifest + 本会话核实 scoop Main/Extras bucket 均无 proc、作者名下无自建 bucket**，两个渠道的安装指引均为失实陈述
   - MCP SSE 段（bind-addr 安全默认之后）加 rmcp RUSTSEC-2026-0189 注记（D4「ignore 期间在 ADR/README 安全叙事如实注记」：stdio 默认不受影响 + SSE 三条件 + TD-63 追踪）

### 6. 搭车：幽灵引用清零 + TD 台账

**ADR-0003 幽灵引用改代码层锚**（验收锚：全仓 grep 人工核对语境，PID 复用语境零残留；smartctl 语境合法引用与冻结历史文档不动）——REVIEW-v0.26 Findings ② 列四处 + 本会话全仓 grep 发现同类四处，共 **8 处**：

| # | 位置 | 处置 |
|---|---|---|
| 1 | `src/app.rs:181`（REVIEW ②） | 改代码锚（深挖导览①） |
| 2 | `CONTEXT.md:39` PID 词条（REVIEW ②） | 同上 |
| 3 | `docs/adr/0021:6` Related（REVIEW ②；含指向不存在文件 `0003-pid-reuse-start-time-key.md` 的死链） | 删死链改代码锚 |
| 4 | `docs/adr/0036:58` D4 表①（REVIEW ②） | 证据源列改代码层五处键控 |
| 5 | `src/mcp/handler/mod.rs:362`（本会话 grep 发现，同类） | 改代码锚 |
| 6-7 | `src/net_flow/mod.rs:14` + `:35`（同类） | 改代码锚 |
| 8 | `src/security/score.rs:221`（同类） | 改代码锚 |

保留不动：`src/smart/mod.rs:3/:11` + `docs/adr/0004`（smartctl 语境实体引用）+ 深挖导览勘误注 + REVIEW/tech-debt/stage docs（发现记录与冻结历史）。

**TD 台账**（`docs/tech-debt.md`）：
- **TD-63 新立**：rmcp 0.11 RUSTSEC-2026-0189 —— 0.11→≥1.4 major 升级（独立 cycle 候选；brainstorm 决策 3A 兑现）
- **v0.27 cycle 追踪段**新立（stage 2 行：TD-63 + 幽灵引用 8 处清零 + winget/scoop 口径核实）
- 历史回顾追加一行

---

## 验证

- ✅ 开工基线：默认档 1744/0/10（80 行；首跑 TD-62 红按口径处置）+ anthropic 档 1768/0/11（80 行；首跑 test_inspect flaky 红按口径处置）+ 三件套全过（fmt / clippy 双档 / build 双档 / bench --no-run）
- 完工验证（全部改动 + lockfile 变更后，2026-09-03 实测）：
  - ✅ 全量回归双档 **1744 / 0 / 10 + 1768 / 0 / 11 不变**（各 80 行核对；env-gate 本地零激活 + crossbeam patch 版本变更不改行为面）
  - ✅ fmt / clippy 双档 / build 双档 / bench --no-run 全过
  - ✅ `cargo +1.88.0 check --no-default-features` 绿（B 方案实证，34.6s；`--all-targets` 变体预存在失败见任务 2 注记——1.95 同样失败确认非本阶段引入）
  - ✅ `cargo audit --ignore RUSTSEC-2026-0189` exit 0（本地预验；唯一 vulnerability 即 rmcp、crossbeam-2026-0204 已随 0.9.20 清除、6 个 warning 级不挡 job）
  - ✅ `git diff Cargo.lock` 仅 crossbeam-epoch 一包（版本 + 校验和两行）
  - ✅ 不变锚：MCP tool **46**（grep 实测）/ agent catalog 47（回归覆盖）/ runtime deps +0（Cargo.toml diff 仅 rust-version + 注释 + [lints]）
  - ✅ 幽灵引用清零锚：`grep -rn "ADR-0003" src/ docs/adr/ CONTEXT.md` 人工核对——PID 复用语境零残留（余项全部为勘误注记 / smartctl 语境 / 冻结历史记录）
  - workflow YAML：删 job / 改 runner 后 ci.yml + release.yml 结构完整（远端运行验证属 stage 3 push 后）
- 远端 CI 全绿是 **stage 3** 验收锚（push 用户拍板）——本阶段完工不 push

## 完工收尾（按手册步 16-23）

- [x] brainstorm 3 stage 总览表勾阶段 2 `[x]`（唯一勾选点）
- [x] CHANGELOG `[Unreleased]` 加 stage 2 条目
- [x] CONTEXT.md：v0.27 术语段三词条（choke-point gate / msrv 双修 / 声明性降级）stage 1 定稿 → stage 2 已落地 + 幽灵引用词条更新（8 处清零 + 完成注记）+ unsafe-audit 词条 miri 口径随 doc 同步
- [x] commit（模板：`fix(v0.27-stage-2): CI 修复主项落地——ADR-0037 D1-D5 实装（choke-point env-gate + ETW 两文件 gate + timeout 60/45 + 删 check-macos + msrv 双修 windows runner + rust-version 1.88 本地 1.88.0 实证 + [lints] collapsible_if 过渡 + audit permissions/crossbeam≥0.9.20/RUSTSEC ignore 预验 exit 0 + miri.yml 删 + winget job 删 + unsafe-audit 措辞同步 + README winget/scoop/miri 口径 + rmcp 注记）+ 搭车（ADR-0003 幽灵引用 8 处改代码层锚 + TD-63 新立 + v0.27 追踪段）；回归双档 1744/0/10 + 1768/0/11 不变 + 三件套全过`）
- [x] 输出阶段完成报告 + 阶段 3 启动指令包（含「建议 push 验证」——push 为用户拍板项）

## 已知风险（本阶段观察）

1. **首次 push 后仍有未知 CI 挂点/失败**（brainstorm 风险 1）：1744 测试在 runner 的行为只能远端验证——时序断言类（perf_baseline / search_perf / scorer sleep）与 kill_by_name 降级路径是 stage 3 观察项；红属预期范围非本阶段失败。
2. ~~本地 cargo audit 预验不可达~~ **已解除**（2026-09-03 复验成功：DB 当日拉取，`cargo audit --ignore RUSTSEC-2026-0189` exit 0——唯一 vulnerability 即 rmcp、crossbeam 已清、6 个 warning 级不挡 job；action.yml ignore 输入已 GitHub API 实证存在）。
3. **`cargo update -p crossbeam-epoch` 的连带面**：预期仅一包变更；若 lockfile diff 出现随动包逐一核对并记录（回归双档重跑兜底）。
4. **开工双档各一起首跑 flaky**（默认档 TD-62 本体 + anthropic 档 `test_inspect::inspect_aggregates_three_buckets`「dlls empty」）——均单跑绿 + 复跑绿，同族负载敏感观察项口径（TD-62 词条已覆盖机制讨论；inspect dlls 枚举首触发留档，复现再评估是否并入 TD 词条）；不影响本阶段「树未变」的基线结论。
