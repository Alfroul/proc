### 阶段 10：批量修复与收尾交付 — 修 P0/P1 + README + CHANGELOG + tag v0.7.0

> ✅ **v0.7.0 已发布（2026-06-28）**。本阶段 = release commit 本身（版本号 bump + CHANGELOG/README/CONTEXT 计划态清理 + release CI 加 `_ebpf` 后缀二进制 + completions 打包 + stage docs 头部标已发布）。Stage 9 Review 跳过。详见 CHANGELOG `[0.7.0]` 段「阶段 10」。

> **独立会话指令**：`阅读 CONTEXT.md、docs/stages/v0.7-stage-10.md、docs/reviews/REVIEW-8.md，修复所有 P0/P1 问题并完成 v0.7.0 项目交付`

**目标**：消费 `docs/reviews/REVIEW-8.md` 全部 P0/P1 问题并修复，完成 v0.7.0 最终交付物（README + CHANGELOG + tag）。本阶段不再开发新功能。

**前置依赖**：阶段 9（Review）已完成。

**开工前必读**：

- `CONTEXT.md` — 全部 v0.7 术语
- `docs/stages/v0.7-stage-10.md` — 本阶段任务
- `docs/reviews/REVIEW-8.md` — 待修复问题清单
- `docs/tech-debt.md` — 验证 TD 状态
- 测试命令

**任务清单**：

1. **修复 REVIEW-8.md 全部 P0 问题（时间不定，按数量）**
   - 每个 P0 单独 commit（便于追溯），commit message 格式：`fix(v0.7.0): P0-N 简短描述`
   - 每修一个 P0 跑一次相关测试
   - 全部 P0 修完后跑全量回归 `cargo test --release -q`
   - 在 REVIEW-8.md 对应条目后追加 `**Status: Fixed in commit XXX**`
   - 验证：REVIEW-8.md 全部 P0 标 Fixed

2. **修复 REVIEW-8.md 全部 P1 问题**
   - 同 P0 流程
   - 验证：REVIEW-8.md 全部 P1 标 Fixed

3. **归档 P2 到 `docs/tech-debt.md`**
   - 把 REVIEW-8.md 的 P2 按 v0.8.0 候选 / v0.9.0+ 候选分组追加到 tech-debt.md
   - 编号继续 v0.6.0 v0.7.0 之后（TD-17 / TD-18 / ...）
   - 验证：tech-debt.md 含 v0.8.0+ 段，至少有 TD-15 / TD-16（v0.7 阶段 8 已留）+ 新追加的

4. **Cargo.toml 版本号 0.6.0 → 0.7.0**
   - 改 `version = "0.6.0"` 为 `"0.7.0"`
   - 验证：`proc --version` 输出 `proc 0.7.0`
   - crash report 含 `version: 0.7.0`
   - `cargo binstall proc` 装到的是 0.7.0

5. **README 完整重写（半天）**
   - 顶部一句话描述加 v0.7.0 三大主题：**生态卡位**（MCP + completion + 命令面板）/ **平台深度**（PSI + EcoQoS + ETW + eBPF）/ **架构债清理**（App 拆 controller）
   - 功能段：补 v0.7.0 新增能力（MCP server / 命令面板 Ctrl+P / 过滤表达式 / PSI / EcoQoS / per-process ETW 磁盘 IO / eBPF flow graph）
   - 平台支持表：加 v0.7.0 新行
   - 快捷键表：加 Ctrl+P / T（EcoQoS toggle） / F（Flow 子视图） / `:`（FilterExpr 模式切换）
   - CLI 子命令表：加 `proc mcp serve` / `proc completions --shell` / `proc throttle <pid>` / `proc flows`
   - FAQ 加：MCP server 怎么用 / eBPF feature flag 怎么开 / FilterExpr 语法
   - 安装段：补 shell completion 安装说明
   - 验证：README 完整反映 v0.7.0 能力

6. **CHANGELOG 定稿（半天）**
   - `## [0.7.0] - 2026-XX-XX`（用今天日期）
   - 按阶段 1-8 列变更，参考 v0.6.0 CHANGELOG 格式
   - **阶段 1**：技术债一波清（列 10 项 TD 简述）
   - **阶段 2**：MCP server（rmcp + 17 tools）
   - **阶段 3**：Shell completion + 命令面板 Ctrl+P
   - **阶段 4**：过滤表达式 FilterExpr（nom parser）
   - **阶段 5**：App 拆 5 panel controller（TD-6）
   - **阶段 6**：PSI 监控 + EcoQoS 切换
   - **阶段 7**：ETW per-process 磁盘 IO
   - **阶段 8**：eBPF flow graph + exit-accounting（feature flag ebpf）
   - **阶段 9**：Review 产出 REVIEW-8.md
   - **阶段 10**：修 P0/P1 + README + CHANGELOG + tag v0.7.0
   - 验证：CHANGELOG 完整反映 v0.7.0

7. **release CI 验证（半天）**
   - `.github/workflows/release.yml` 检查：
     - 5 个 target 仍能 build（win-msvc / linux-musl / linux-arm / macos-arm / macos-x86）
     - linux-musl 和 linux-arm target 是否额外产出 `_ebpf` 后缀二进制（带 `--features ebpf`）
     - completions/ 目录是否打包进 release zip/tar
   - 推一个测试 tag `v0.7.0-rc1` 看 release workflow 是否完整跑通（draft release）
   - 验证：5 个 target build 通过，completions 在产物里

8. **winget / scoop 模板更新（1 h）**
   - `winget-pkgs-templates/` 检查版本号同步
   - `scoop/proc.json` 检查版本号 + completions 路径
   - 验证：模板版本号是 0.7.0

9. **打 tag v0.7.0（5 min）**
   - `git tag -a v0.7.0 -m "v0.7.0: 生态卡位 + 平台深度 + 架构债清理"`
   - **不立刻 push tag**（让用户确认后再 push，push 触发 release workflow）
   - 验证：`git tag --list "v0.7*"` 含 v0.7.0

10. **启动验证（半天）**
    - 按 plan.md「快速开始」段全部跑一遍：
      - `cargo build --release`（无 feature）
      - `cargo build --release --features ebpf`（Linux only，验证 feature flag）
      - `cargo build --release --no-default-features`（验证 cfg-gate）
      - `cargo test --release -q`（全量）
      - `cargo fmt --all -- --check` + `cargo clippy --release --all-targets -- -D warnings`
      - `./target/release/proc`（TUI 启动）
      - `./target/release/proc ls --sort cpu --limit 20`
      - `./target/release/proc mcp serve`（启动后等 stdio 输入，用 mcp-inspector 连）
      - `./target/release/proc completions --shell bash`
      - `./target/release/proc throttle <pid> on`（Windows only）
      - `./target/release/proc flows`（Linux + ebpf feature only）
    - 验证：所有命令按文档执行无报错

11. **`docs/tech-debt.md` 终态确认（30 min）**
    - v0.6.0 P2（14 项）：10 项阶段 1 Fixed + TD-6 阶段 5 Fixed + TD-11 决策不修 + TD-12/13 推 v0.8.0 + TD-14 阶段 1 Fixed
    - v0.7.0 阶段 9 新 P2：追加到 v0.8.0+ 段
    - v0.7.0 阶段 8 留的 TD-15/16：v0.8.0+ 候选
    - 验证：tech-debt.md 状态清晰

12. **删除可能残留的 Checkpoint 文件（5 min）**
    - `ls docs/handoff-stage-*-checkpoint.md` 应无结果（v0.7 各阶段完工时已删）
    - 如有残留 → 删除
    - 验证：grep handoff 无结果

13. **最终自检（30 min）**
    - `cargo test --release -q` 全部通过
    - `cargo fmt --all -- --check` 通过
    - `cargo clippy --release --all-targets -- -D warnings` 0 warnings
    - `git log --oneline v0.6.0..HEAD` 应有 10 个阶段的 commit（每个阶段 1-5 个 commit）
    - plan.md 阶段总览 1-10 全部 `[x]`
    - 验证：所有检查通过

**验收标准**：

- 全部 P0/P1 问题修复（REVIEW-8.md 标 Fixed）
- `cargo test --release -q` 全部通过（≥ 760+ passed，0 failed）
- `cargo fmt --all -- --check` / `cargo clippy --release --all-targets -- -D warnings` 通过
- `cargo build --release --no-default-features` 编译通过
- `cargo build --release --features ebpf`（Linux）编译通过
- Cargo.toml 版本号是 0.7.0
- README + CHANGELOG 完整反映 v0.7.0
- `git tag v0.7.0` 已打（未 push）
- winget/scoop 模板同步
- 启动验证全部通过
- tech-debt.md 终态正确
- Checkpoint 文件全部清理

**最终交付物清单**：

- ✅ 10 个阶段全部完成（plan.md 1-10 全 `[x]`）
- ✅ `docs/reviews/REVIEW-8.md` + `docs/tech-debt.md` 终态
- ✅ `docs/adr/0009-0016` 8 个新 ADR（Status: Accepted）
- ✅ README + CHANGELOG + CONTRIBUTING + SECURITY
- ✅ Cargo.toml 版本 0.7.0
- ✅ git tag v0.7.0（待用户确认 push）
- ✅ release CI 5 个 target + ebpf 二进制 + completions
- ✅ 全量回归 760+ tests passed

**容量预估**：~1000-2000 行（README 500 行 + CHANGELOG 500 行 + P0/P1 修复 500-1000 行），工具调用 60-100 次。**注意**：P0/P1 数量未知，如超过 15 项可能需要拆分修复。
