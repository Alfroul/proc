### 阶段 2：Slice — 录屏书签系统（键位 `b` 添加 / `B` 列表 + `.prec.bookmarks.json` sidecar）

> **独立会话指令**：`阅读 CONTEXT.md 和 docs/stages/v0.14-stage-2.md，完成所有任务后确认完成`
>
> **开工前只需阅读**：`CONTEXT.md`（领域词汇；v0.13 演进历史 + v0.14 阶段 1 行）、`docs/stages/v0.14-brainstorm.md`（cycle 总览，方案 A 5 stage）、`docs/stages/v0.14-stage-1.md`（格式 v3 已落地，stage 2 在 v3 之上建书签）、`docs/stages/v0.14-stage-2.md`（当前阶段任务）。不需要读其他 cycle 的 stage docs。

**目标**：录屏书签系统 — 录制时按 `b` 标记关键帧（CPU 飙升 / anomaly 触发 / docker 重启 等用户关注的事件），回放时按 `B` 打开书签列表，跳转 / 编辑 / 删除书签。书签持久化到 `.prec.bookmarks.json` sidecar，与录屏文件解耦（可单独分享 / 删除，不污染录屏本体）。

**前置依赖**：v0.14 stage 1 已落地（commit `dc96ae8`），录屏文件格式 v3（按需加载 + footer）可用。`Player::path()` / `Player::header()` 暴露录屏元信息；`VtPlayer::header()` 同步可用。

**预期工作量**：~400 行（`bookmark.rs` 类型 + sidecar IO + 录制 `b` 键 + 回放 `B` 键 + replay_panel modal + VT100 replay `B` 键 + 测试）。

**依赖测试**（开工时跑这些命令，任一失败先修复）：
- `cargo test --release -q 2>&1 | grep "^test result:" | awk '{p+=$4; f+=$6; i+=$8} END {print p" passed / "f" failed / "i" ignored"}'`（v0.14 stage 1 后基线：**1135 passed / 0 failed / 3 ignored**）
- `cargo fmt --all -- --check`
- `cargo clippy --release --all-targets -- -D warnings`
- `cargo build --release --no-default-features`
- `cargo bench --no-run`

---

## ROI 来源（brainstorm §子方向 2：书签系统）

**现状**：录屏启动后用户看到关键事件（如 CPU 飙升 / anomaly 触发 / docker 重启），无法标记。回放时只能从头看到尾，无法跳转到关键事件。

**stage 2 落地效果**：
- 录制时按 `b` → 当前帧被标记（写入 in-memory list，停止录制时 flush 到 `.prec.bookmarks.json`）
- 回放时按 `B` → 打开书签面板，Up/Down 选择，Enter 跳转到目标帧，`e` 编辑 label，`d` 删除，Esc 关闭
- 书签与录屏文件解耦：同一录屏可有多组书签（个人 / 团队 / 客户），分享录屏时不带书签也不影响

**与 stage 1 的衔接**：stage 1 的 v3 footer 让回放 seek 单帧 = 165 µs（@ 1000 进程），书签跳转无感延迟。stage 1 的 `Player::path()` 暴露录屏路径，stage 2 直接基于路径定位 sidecar。

---

## 任务清单

### 1. `src/record/bookmark.rs`（新）：类型 + sidecar IO

```rust
//! 录屏书签系统：录制时按 `b` 添加书签，回放时按 `B` 打开列表跳转 / 编辑 / 删除。
//!
//! 持久化到 `.prec.bookmarks.json` sidecar（与录屏同名 `.prec.bookmarks.json`），
//! 与录屏本体解耦（不污染录屏文件，可单独分享 / 删除）。
//!
//! 设计原则（同 `.prec.idx` sidecar）：
//! - 录屏文件**永不重写**（书签变动只动 sidecar）
//! - sidecar 损坏静默降级（fallback 到空书签列表）
//! - sidecar schema 演进靠 `version` 字段 bump

use std::path::{Path, PathBuf};
use serde::{Deserialize, Serialize};

const BOOKMARK_MAGIC: [u8; 8] = *b"PRECBMK\x01";

/// 单个书签条目（一帧对应一书签）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Bookmark {
    /// 自增 ID（在单个录屏范围内唯一）。
    pub id: u64,
    /// 录屏帧索引（positional，0-based；跳转时调 `player.frame_at(idx)`）。
    pub frame_idx: usize,
    /// 该帧的绝对 unix epoch 秒（用于显示 + 跨录屏排序）。
    pub timestamp_secs: u64,
    /// 用户输入 / 默认生成的 label（"书签 #N"）。
    pub label: String,
    /// 书签创建时间（unix epoch 秒）。
    pub created_at: u64,
}

/// 书签 sidecar 文件（与录屏同名 `.prec.bookmarks.json`）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BookmarkFile {
    pub magic: [u8; 8],
    pub version: u16,
    /// 关联录屏路径（debug 用，不参与新鲜性校验）。
    pub source_path: String,
    /// 录屏文件大小（字节）— 新鲜性校验用。
    pub source_size: u64,
    /// 录屏文件 mtime（unix epoch 秒）— 新鲜性校验用。
    pub source_mtime: u64,
    /// 全部书签（按 frame_idx 升序）。
    pub bookmarks: Vec<Bookmark>,
}

impl BookmarkFile {
    /// `recording.prec` → `recording.prec.bookmarks.json`。
    #[must_use]
    pub fn sidecar_path(prec_path: &Path) -> PathBuf {
        let mut s = prec_path.as_os_str().to_os_string();
        s.push(".bookmarks.json");
        PathBuf::from(s)
    }

    /// 尝试加载 sidecar：文件不存在 / 损坏 / magic 不匹配 / 录屏已变化 都返 `None`。
    /// 调用方 fallback 到空书签列表（不致命）。
    pub fn try_load(prec_path: &Path) -> Option<Self> {
        let sidecar_path = Self::sidecar_path(prec_path);
        let text = std::fs::read_to_string(&sidecar_path).ok()?;
        let file: BookmarkFile = serde_json::from_str(&text).ok()?;
        if file.magic != BOOKMARK_MAGIC { return None; }
        // 新鲜性：录屏文件 size + mtime 必须匹配
        let meta = std::fs::metadata(prec_path).ok()?;
        let size = meta.len();
        let mtime = meta.modified().ok()?
            .duration_since(std::time::UNIX_EPOCH).ok()?
            .as_secs();
        if file.source_size != size || file.source_mtime != mtime {
            return None;
        }
        Some(file)
    }

    /// 写 sidecar（JSON pretty）。失败静默（用户目录只读不致命）。
    pub fn write(&self, prec_path: &Path) {
        let sidecar_path = Self::sidecar_path(prec_path);
        let Ok(text) = serde_json::to_string_pretty(self) else { return; };
        let _ = std::fs::write(sidecar_path, text);
    }

    /// 构造空 sidecar（首次 open 录屏 / sidecar 不存在时用）。
    #[must_use]
    pub fn empty_for(prec_path: &Path) -> Self {
        let (size, mtime) = std::fs::metadata(prec_path)
            .map(|m| {
                let size = m.len();
                let mtime = m.modified().ok()
                    .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
                    .map(|d| d.as_secs())
                    .unwrap_or(0);
                (size, mtime)
            })
            .unwrap_or((0, 0));
        Self {
            magic: BOOKMARK_MAGIC,
            version: 1,
            source_path: prec_path.to_string_lossy().into_owned(),
            source_size: size,
            source_mtime: mtime,
            bookmarks: Vec::new(),
        }
    }

    /// 加 sidecar 如不存在；存在则 try_load；损坏 / 过期返 empty。
    #[must_use]
    pub fn load_or_empty(prec_path: &Path) -> Self {
        Self::try_load(prec_path).unwrap_or_else(|| Self::empty_for(prec_path))
    }

    /// 添加书签（id 自增；frame_idx 不要求单调，调用方决定）。
    pub fn add(&mut self, frame_idx: usize, timestamp_secs: u64, label: String, now: u64) -> &Bookmark {
        let next_id = self.bookmarks.iter().map(|b| b.id).max().unwrap_or(0) + 1;
        let bm = Bookmark {
            id: next_id,
            frame_idx,
            timestamp_secs,
            label,
            created_at: now,
        };
        self.bookmarks.push(bm.clone());
        self.bookmarks.last().unwrap()
    }

    /// 按 id 删除书签，返回是否删除成功。
    pub fn remove(&mut self, id: u64) -> bool {
        let before = self.bookmarks.len();
        self.bookmarks.retain(|b| b.id != id);
        self.bookmarks.len() != before
    }

    /// 按 id 编辑书签 label。
    pub fn edit_label(&mut self, id: u64, new_label: String) -> bool {
        if let Some(b) = self.bookmarks.iter_mut().find(|b| b.id == id) {
            b.label = new_label;
            true
        } else {
            false
        }
    }

    /// 按 frame_idx 升序排序（UI 展示用）。
    pub fn sort_by_frame(&mut self) {
        self.bookmarks.sort_by_key(|b| b.frame_idx);
    }
}
```

**单元测试**：serde round-trip + sidecar_path + try_load (missing / corrupt / wrong magic / stale) + add/remove/edit_label + sort_by_frame + load_or_empty fallback。

**集成 `src/record/mod.rs`**：加 `pub mod bookmark;` + `pub use bookmark::{Bookmark, BookmarkFile};`。

### 2. 录制键位 `b`：App 加 in-memory 书签 + 录制停止时写 sidecar

**`src/app.rs` 改动**：

字段新增（`App` struct）：
```rust
/// 录制中的书签（VT100 录制路径）。
/// 停止录制时序列化到 `.prec.bookmarks.json` sidecar。
recording_bookmarks: Vec<crate::record::Bookmark>,
/// 下一本书签 ID（自增）。
recording_bookmark_next_id: u64,
/// 当前录屏文件路径（VT100 recorder 启动时由 tui::run_app 注入，
/// 让 stage 2 的 stop hook 写 sidecar 用）。
recording_path: Option<std::path::PathBuf>,
/// 当前录屏已写出的帧数（VtRecorder 每次成功 capture 时由 tui::run_app
/// 调 set_recording_frame_count 更新 — 用于 `b` 键标记 frame_idx）。
recording_frame_count: usize,
/// `b` 键打开的 inline label 输入状态：None=未激活 / Some=激活中。
/// 用户按 Enter 提交 / Esc 取消。
pending_bookmark_label: Option<PendingBookmarkLabel>,
```

`PendingBookmarkLabel`（App 内部 struct，App 模块可见）：
```rust
pub struct PendingBookmarkLabel {
    pub frame_idx: usize,           // 标记瞬间的帧索引
    pub timestamp_secs: u64,        // 标记瞬间的 unix epoch 秒
    pub input: String,              // 用户输入 buffer（可空 → 默认 "书签 #N"）
}
```

App::new() 默认：所有新字段默认值（`Vec::new()` / `0` / `None`）。

App 公开方法：
```rust
pub fn recording_bookmarks(&self) -> &[Bookmark] { &self.recording_bookmarks }
pub fn set_recording_path(&mut self, p: std::path::PathBuf) { self.recording_path = Some(p); }
pub fn set_recording_frame_count(&mut self, n: usize) { self.recording_frame_count = n; }
pub fn pending_bookmark_label(&self) -> Option<&PendingBookmarkLabel> { self.pending_bookmark_label.as_ref() }
pub fn pending_bookmark_label_mut(&mut self) -> Option<&mut PendingBookmarkLabel> { self.pending_bookmark_label.as_mut() }
```

`b` 键流程（`handle_key` 在录制模式下捕获 `b`）：
- 当前在录制中（`self.recording_wanted == true`）+ 不在 replay / confirm 模式 + `pending_bookmark_label.is_none()`
- 按 `b` → 创建 `PendingBookmarkLabel { frame_idx: self.recording_frame_count, timestamp_secs: now, input: String::new() }`，set status "标记书签：输入 label，Enter 提交 / Esc 取消"
- 在 inline label 激活状态下：所有字符 keydown → push 到 `input`（除 Enter / Esc / Backspace）
- Enter → 取 input（空 → 默认 "书签 #N"，N = next_id）；push 到 `recording_bookmarks`；`recording_bookmark_next_id += 1`；set status "已加书签 #N (帧 {frame_idx})"
- Esc → 取消，set status "书签标记取消"
- Backspace → pop input 末字符

**`b` 键不能与既有键冲突**：
- 详情页 `b` 已用于「跳到底部」（无单键 b，归 Ctrl+B/F 等）；实测无冲突，但保险起见在详情页 / container exec / search 模式下不激活
- 仅在录制中 + 普通面板（ProcessList / PortMap / MonitorPanel / DockerPanel / UsbAssistant）下激活
- replay / pending_record_confirm / pending_kill / search 输入态下不激活

**录制停止 hook**（`tui::run_app` 检测 `app.recording_wanted()` 由 true → false 时调）：
```rust
// 在 tui/mod.rs::run_app 的 lifecycle 段
if !app.recording_wanted() && was_recording {
    // flush 书签到 sidecar
    if let Some(path) = app.take_recording_path_for_flush() {
        let mut file = BookmarkFile::load_or_empty(&path);
        for bm in app.take_recording_bookmarks() {
            file.bookmarks.push(bm);
        }
        file.sort_by_frame();
        file.write(&path);
    }
}
```

**`VtRecorder` 加 `frame_count()` getter**（`src/record/vt100.rs`）：writer 线程内部 `AtomicU64`，每写一帧 fetch_add；主线程读 atomic load。让 `set_recording_frame_count` 由 `tui::run_app` 每 tick 注入。

### 3. 回放键位 `B`：ReplayController + UiFrame 路径

**`src/replay/controller.rs` 字段新增**：
```rust
pub struct ReplayController {
    pub replay_player: Option<Player>,
    pub timeline_state: Option<TimelineState>,
    /// v0.14 stage 2：书签 sidecar（录屏 open 时一次性 load）。
    pub bookmarks: Option<BookmarkFile>,
    /// `B` 键打开的书签面板状态。None=未打开 / Some=打开中。
    pub bookmark_panel: Option<BookmarkPanelState>,
}

pub struct BookmarkPanelState {
    pub cursor: usize,           // 在 bookmarks.bookmarks Vec 中的索引
    pub search_query: String,    // 子串过滤（空 = 显示全部）
    pub editing_label: Option<String>, // `e` 键激活 inline edit
    pub editing_id: Option<u64>,       // 正在编辑的 bookmark id
}
```

**`ReplayController::start(player)`** 流程加：
1. 拿 player.path() → `BookmarkFile::load_or_empty(path)` → 写到 `self.bookmarks = Some(...)`
2. `bookmark_panel = None`（默认未打开）

**`handle_key` 加分支**：
- 如果 `bookmark_panel.is_some()`（面板已打开）→ 走面板键位
  - `Esc` → 关闭面板（`bookmark_panel = None`）
  - `Up` / `Down` → 移动 cursor（clamp 在过滤后列表范围内）
  - `Enter` → 拿 `bookmarks[cursor].frame_idx` → 设 `ts.current_frame = idx` → return `ReplayAction::ApplyFrame`
  - `d` → 删除 cursor 处书签 → 写 sidecar → 调整 cursor
  - `e` → 进入 edit 模式（`editing_label = Some(current_label.clone())` + `editing_id = Some(id)`）
  - edit 模式下：字符 push 到 `editing_label`，Enter 提交（`bookmarks.edit_label(id, new)` + 写 sidecar），Esc 取消
  - 其他字符 push 到 `search_query`（搜索过滤），Backspace pop
- 否则（面板未打开）→ `B` (Shift+B) 打开面板：`bookmark_panel = Some(BookmarkPanelState { cursor: 0, search_query: String::new(), editing_label: None, editing_id: None })`，set status "书签面板：Up/Down 选择，Enter 跳转，e 编辑，d 删除，Esc 关闭"
- 既有键位（q / space / Left / Right / + / - / Home / End）保留，`b` 不冲突（仅 Shift+B 开面板）

**新 ReplayAction 变体**（可选）：
- `ReplayAction::BookmarkJump(usize)` — 等价 ApplyFrame 但带书签上下文（status_message 区分）
- 简化方案：直接复用 ApplyFrame，由 App 在 `dispatch_replay_action` 中查 `self.replay.bookmark_panel` 是否刚跳转

stage 2 走简化方案（避免新增 action 变体）。

**Sidecar 持久化**：每次 add/edit/delete 后调 `bookmarks.write(player.path())`；写失败静默（用户目录只读不致命）。

### 4. `src/tui/replay_panel.rs` 加书签面板渲染

modal 浮层（占 60% × 50% 屏幕居中）：
- 顶部标题 " 书签 (N) — Up/Down 选择 · Enter 跳转 · e 编辑 · d 删除 · Esc 关闭 "
- 中间是 List：每行 `[id] [frame_idx] [timestamp] [label]`
  - cursor 行高亮（反色）
  - 编辑中行末追加光标 `_`
- 底部是 search query 行 "搜索: {query}_"（仅当 search 非空或正在输入时显示）

**`ratatui::widgets::List` + `ListItem`** 渲染；filter = `bookmarks.bookmarks.iter().filter(|b| b.label.contains(&query) || b.frame_idx.to_string().contains(&query))`。

**集成 `draw_timeline`**：在 timeline 渲染完后，如果 `app.replay.bookmark_panel.is_some()` → 在外层 area 上叠加 modal（用 `clear + CenteredRect` 模式，参考 `command_palette.rs`）。

### 5. VT100 回放路径 `B` 键（`src/tui/mod.rs::run_vt_replay`）

VT100 replay 是独立循环（`run_vt_replay`），与 UiFrame replay 不同路径。stage 2 在 `ReplayState` 加同款 `bookmarks: Option<BookmarkFile>` + `bookmark_panel: Option<BookmarkPanelState>` 字段；`run_vt_replay` 入口 load sidecar；handle_vt_replay_key 加同款分支；`draw_vt_replay_frame` 渲染 modal。

复用 `bookmark.rs` + `BookmarkPanelState`（提升到 `crate::record::bookmark::BookmarkPanelState` 让两侧共用）。

### 6. 测试（`tests/test_bookmark.rs` 新）

```rust
// 类型 + sidecar IO
#[test]
fn bookmark_serde_roundtrip() { /* Bookmark + BookmarkFile 序列化反序列化 */ }

#[test]
fn sidecar_path_appends_bookmarks_json_suffix() { /* .prec → .prec.bookmarks.json */ }

#[test]
fn try_load_missing_returns_none() { /* 文件不存在 */ }

#[test]
fn try_load_corrupt_returns_none() { /* 非 JSON 字节 */ }

#[test]
fn try_load_wrong_magic_returns_none() { /* magic 不匹配 */ }

#[test]
fn try_load_stale_returns_none_when_source_size_differs() { /* 录屏改了 sidecar 过期 */ }

#[test]
fn try_load_stale_returns_none_when_source_mtime_differs() { /* 录屏 mtime 变 */ }

#[test]
fn add_assigns_increasing_ids() { /* next_id 单调 */ }

#[test]
fn remove_by_id_works() { /* 删除指定书签 */ }

#[test]
fn edit_label_by_id_works() { /* 编辑 label */ }

#[test]
fn load_or_empty_creates_empty_when_missing() { /* fallback 到 empty */ }

#[test]
fn write_then_load_preserves_bookmarks() { /* 完整 round-trip */ }

// 集成：录制时 sidecar 写入
#[test]
fn recording_b_key_appends_bookmark() {
    /* 模拟 App：set_recording_frame_count(42) → 模拟 b 键 →
       recording_bookmarks 应有 1 条 frame_idx=42 的书签 */
}

// 集成：回放时 sidecar 加载
#[test]
fn replay_loads_existing_sidecar_on_start() {
    /* 写一个带 sidecar 的 .prec → Player::open → ReplayController::start →
       bookmarks.bookmarks.len() == expected */
}
```

**目标**：1135 + 新 case → **≥ 1148 passed / 0 failed / 3 ignored**。

### 7. 文档同步

- `CONTEXT.md` 「术语演进历史」加 v0.14 阶段 2 行（书签系统落地）
- `CONTEXT.md` 「Replay」段加 `Bookmark` / `BookmarkFile` 术语
- `docs/stages/v0.14-brainstorm.md` cycle 总览表 stage 2 状态 ⬜ → ✅
- `CHANGELOG.md` `[Unreleased]` 段加书签系统条目（v0.14.0 cycle 末尾 stage 5 改 `[0.14.0]`）
- `README.md` 录屏章节提书签功能（用户面向）

---

## 验收标准

### 必须（本 stage 完成条件）

1. ✅ `cargo test --release` ≥ 1148 passed / 0 failed / 3 ignored（基线 1135 + 新测试 ≥ 13）
2. ✅ `cargo fmt --all -- --check` 通过
3. ✅ `cargo clippy --release --all-targets -- -D warnings` 通过
4. ✅ `cargo build --release --no-default-features` 通过
5. ✅ `cargo bench --no-run` 通过
6. ✅ `BookmarkFile` serde JSON round-trip + sidecar load/save 正确性（单元测试覆盖）
7. ✅ sidecar 损坏 / 缺失 / magic 错误 / 录屏变化时静默降级到空书签（单元测试覆盖）
8. ✅ 录制时按 `b` 输入 label Enter 添加书签；停止录制后 sidecar 文件生成
9. ✅ 回放时按 `B` 打开书签面板，Up/Down / Enter / `e` / `d` / Esc 全部工作
10. ✅ 书签 add/edit/delete 后 sidecar 持久化（下次 open 能看到变更）

### 不在本 stage 范围

- ❌ 时间轴搜索（FilterExpr 扩 timestamp / cpu / anomaly — stage 3）
- ❌ 倒放（stage 4）
- ❌ REVIEW-v0.14 + tag v0.14.0（stage 5）
- ❌ 书签 sidecar 加密 / 用户 ACL（用户场景不需要）
- ❌ 跨录屏书签聚合（用户场景未明确）
- ❌ MCP 暴露 `proc_bookmark_*` tools（ADR-0009 决定不暴露 MCP record/replay，不动）

---

## 已知风险与回退

**风险 1：`b` 键冲突既有面板**
- 详情页：无单键 `b`（Ctrl+B/F 跳转用 Ctrl）；search 输入态字符 `b` 是搜索内容（不激活）
- App `b` 仅在 `recording_wanted == true && pending_bookmark_label.is_none() && !in_search_input()` 时激活
- 缓解：tests/test_app_recording.rs（如存在）或新 case 验证 `b` 在不同 mode 下的行为

**风险 2：sidecar schema 演进**
- BookmarkFile 含 `version: u16`；未来字段添加 bump version，旧 reader 拒绝 → fallback empty
- 缓解：单元测试 `try_load_wrong_version_returns_none`

**风险 3：录制中 user kill proc（Ctrl+C）sidecar 未 flush**
- proc 主进程被 kill 时 tui::run_app 退出循环，最后一段 `vt_recorder.take().stop()` + `app.shutdown()` 不调 flush
- 缓解：在 tui::run_app 末尾的 cleanup 段加 `flush_bookmarks_if_any(app)`（无条件调一次，安全 no-op 当无书签）
- Drop semantics：`App` 持 `recording_bookmarks: Vec<Bookmark>`，Drop 时不会自动写 sidecar（Vec Drop 不触发 IO），需显式 flush

**风险 4：VT100 录制帧索引 vs UiFrame 帧索引语义不同**
- VT100：每 200ms（5 FPS）capture 一帧，frame_idx 是 capture 计数
- UiFrame：每 50ms（理论 20 FPS）submit 一帧，frame_idx 是 submit 计数
- 书签 frame_idx 在录制时记录的是「当时的 frame_count」，跳转时 `player.frame_at(idx)` 直接对齐
- 边界：用户按 `b` 时 frame_count=N 但实际录屏写出 N+1 帧（capture 还在 in-flight）→ 跳转时 frame_at(N) 仍有效（最后一帧索引）

**回退策略**：stage 2 失败 → `git checkout` 还原 `src/record/mod.rs` / `src/app.rs` / `src/replay/controller.rs` / `src/tui/{mod,replay_panel}.rs` → stage 2 重做。stage 2 不依赖 stage 3/4 任何代码，独立可回退。

---

## 独立会话指令（开工时复制）

```
阅读 CONTEXT.md 和 docs/stages/v0.14-stage-2.md，完成所有任务后确认完成

开工前需读：
- CONTEXT.md（领域词汇；v0.13 + v0.14 阶段 1 演进历史）
- docs/stages/v0.14-brainstorm.md（cycle 总览，方案 A 5 stage）
- docs/stages/v0.14-stage-1.md（格式 v3 已落地，stage 2 在 v3 之上建书签）
- docs/stages/v0.14-stage-2.md（当前阶段任务）

开工命令（任一失败先修复）：
- cargo test --release -q 2>&1 | grep "^test result:" | awk '{p+=$4; f+=$6; i+=$8} END {print p" passed / "f" failed / "i" ignored"}'（仍应 ≥ 1135 passed / 0 failed / 3 ignored，v0.14 stage 1 后基线）
- cargo fmt --all -- --check
- cargo clippy --release --all-targets -- -D warnings
- cargo build --release --no-default-features
- cargo bench --no-run

预期 stage 2 工作量：~400 行（bookmark.rs 类型 + sidecar IO + 录制 b 键 + 回放 B 键 + replay_panel modal + VT100 replay B 键 + 测试）。
```

---

## stage 2 完成后下一步

stage 2 完工后：
1. commit（消息模板：`feat(v0.14-stage-2): 录屏书签系统（b 键添加 + B 键列表 + .bookmarks.json sidecar + VT100 同步）`）
2. **不打 v0.14.0 tag**（cycle 末尾 stage 5 才 tag）
3. 在本会话末尾产出简短总结，让用户开新会话启动 stage 3（时间轴搜索）
