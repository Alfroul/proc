### 阶段 8：批量修复与收尾交付

> ✅ **已完成**（v0.11.0 阶段 8 会话产出，2026-07-01；本次会话产出）

> **独立会话指令**：`阅读 CONTEXT.md、docs/stages/v0.11-stage-8.md、docs/reviews/REVIEW-13.md，修复所有 P0/P1 问题并完成 v0.11.0 交付`

**目标**：消费 Review 报告修复阻断性问题，完成 v0.11.0 最终交付物。本阶段不再开发新功能。

**前置依赖**：阶段 7（Review）已完成，`docs/reviews/REVIEW-13.md` 已产出。

**任务清单**：

1. **修复 P0 问题**（来自 `docs/reviews/REVIEW-13.md`）
   - 逐项修复，每项修复后跑相关测试
   - P0 = 0 项时跳过本步

2. **修复 P1 问题**
   - 逐项修复，每项修复后跑相关测试
   - 单独 commit `fix(v0.11.0): REVIEW-13 P1-N <description>`（参考 v0.10.0 / v0.8.0 模式）

3. **全量回归测试**
   - `cargo test --release -q` 全部通过，≥ 1010 passed（含 P1 修复新增测试）
   - `cargo fmt --all -- --check` 通过
   - `cargo clippy --release --all-targets -- -D warnings` 通过
   - `cargo build --release --no-default-features` 通过

4. **归档 P2 问题**
   - 在 `docs/tech-debt.md` 加 v0.12.0+ 候选段
   - 把 REVIEW-13.md 的 P2 转为 TD-23 / TD-24 / ... 格式
   - 每项含：位置 / 现状 / 影响 / 修复 / 验证 / 决策（修 / 不修 + 理由）

5. **版本号 bump 0.10.0 → 0.11.0**
   - `Cargo.toml` 改 version
   - `cargo build --release` 让 Cargo.lock 同步
   - 跳过 v0.9 是 v0.10 cycle 决策；v0.11 不跳号

6. **CHANGELOG 加 v0.11.0 段**
   - 把 `[Unreleased]` 改为 `## [0.11.0] - YYYY-MM-DD`
   - 加 8 个阶段汇总段（与 v0.10.0 / v0.8.0 同款结构）
   - 「已知限制」段：Win10 < 1809 admin 下 Schannel event 1793 不 fire（延续 v0.10）；worker restart 3 次失败后仍永久死亡（catch_unwind + crash report 路径）

7. **README 更新**
   - banner 加 v0.11.0 段（worker restart / DNS ETW / FilterExpr v2 / 签名验证 + R16 / 父子链 + R17 / 可疑路径 R18）
   - 平台支持表更新：
     - 「DNS 日志采集」Windows 行从「PowerShell Get-WinEvent」改为「ETW（默认）/ PowerShell（fallback）」
     - 「进程签名验证」Windows 行加 ✅ WinVerifyTrust
     - 「安全评分」N 项 → 18 项
   - FAQ 加：worker restart 行为 / DNS ETW vs PowerShell / R16/R17/R18 触发条件

8. **CONTEXT.md 更新**
   - 「术语演进历史」段已含 v0.11.0 阶段 1-6 行（每阶段产出时已加）
   - 加 v0.11.0 阶段 7（review）/ 阶段 8（release）行
   - v0.10.0 段从「开发中」改为「已发布」

9. **4 个 stage docs 头部加 ✅ 已发布标记**
   - `docs/stages/v0.11-stage-{1..6}.md` 头部加 `> ✅ **已发布**（v0.11.0，YYYY-MM-DD）`
   - `docs/stages/v0.11-stage-7.md` 头部加 `> ✅ **已发布**（v0.11.0，YYYY-MM-DD；本次会话产出）`
   - `docs/stages/v0.11-stage-8.md`（本文件）头部加 ✅

10. **tech-debt 终态确认**
    - TD-4 ✅ Fixed in v0.11.0 阶段 1
    - TD-1 ~ TD-22 状态保持（v0.10 之前的 TD 都已 Fixed 或归档）
    - 新加 TD-23 / TD-24 / ...（来自 REVIEW-13 P2 归档）

11. **打 tag v0.11.0**
    - `git tag -a v0.11.0 -m "v0.11.0：worker restart + DNS ETW + FilterExpr v2 + 签名验证 R16 + 父子链 R17 + 可疑路径 R18"`
    - 不 push，等用户确认

12. **启动验证**（admin only，Windows）
    - `cargo build --release`
    - `./target/release/proc.exe` —— 启动 TUI，确认 worker restart banner / DNS ETW 数据 / 签名标记 / 父子链 Inspector / R16/R17/R18 评分
    - `./target/release/proc.exe ls --filter 'security_score < 80'` —— 验证未签名 / 可疑路径 / 可疑链进程被扣分
    - `./target/release/proc.exe diag` —— 确认 `dns_collector: etw` + worker restart metrics
    - 非管理员降级路径：worker restart 仍工作；DNS ETW → PowerShell fallback；签名验证 → Unknown；R16 不扣分

**验收标准**：
- REVIEW-13.md 所有 P0 / P1 标 Fixed（在 REVIEW 文件中标注 commit）
- `cargo test --release -q` ≥ 1010 passed / 0 failed（v0.10.0 基线 959 → +51 v0.11.0 cycle 累计）
- Cargo.toml 版本号 0.11.0
- README + CHANGELOG + CONTEXT 完整反映 v0.11.0
- `git tag v0.11.0` 已打（未 push）
- tech-debt TD-4 ✅ Fixed；新 P2 归档到 v0.12.0+ 候选段
- Windows admin 实跑 proc 全部新功能正常

**容量预估**：~400 行（P1 修复 200 + README/CHANGELOG 200），工具调用 30-50 次。

---

**最终交付物清单**（完工后输出，无下一阶段）：

```
═══════════════════════════════════════════════════
✅ v0.11.0 项目交付完成。

【最终交付物】
- Cargo.toml: 0.11.0
- CHANGELOG.md: [0.11.0] 段完整
- README.md: banner + 平台表 + FAQ 三处更新
- CONTEXT.md: v0.11.0 段完整（含术语演进历史 8 行）
- docs/adr/: 0019 / 0020 / 0021 三条新 ADR（Accepted）
- docs/stages/v0.11-stage-{1..8}.md: 全部 ✅ 已发布标记
- docs/reviews/REVIEW-13.md: P0/P1 全 Fixed
- docs/tech-debt.md: TD-4 ✅ Fixed；新 P2 归档 TD-23+
- git tag v0.11.0: 已打（未 push）

【基线测试】
- cargo test --release -q: N passed / 0 failed / 3 ignored
- cargo fmt --all -- --check: 通过
- cargo clippy --release --all-targets -- -D warnings: 0 warnings
- cargo build --release --no-default-features: 通过

【v0.11.0 cycle 摘要】
- 阶段 1：Worker Restart（TD-4 Fixed）+ ProcessInfo 字段骨架
- 阶段 2：DNS ETW 替代 PowerShell probe
- 阶段 3：FilterExpr v2 网络字段接入
- 阶段 4：进程签名验证 + R16
- 阶段 5：进程父子链 + R17
- 阶段 6：可疑路径 R18
- 阶段 7：全局 Review
- 阶段 8：批量修复 + tag

【下一步候选（v0.12.0+）】
- TD-17 eBPF TLS SNI / JA4（Linux 真机环境依赖）
- TD-19 eBPF Linux 真实编译验证
- v0.11 REVIEW-13 P2 归档项
- 新方向：参考 v0.10-stage-4.md 末尾候选

请用户确认交付后 push tag v0.11.0：
  git push origin v0.11.0

═══════════════════════════════════════════════════
```
