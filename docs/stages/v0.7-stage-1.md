### 阶段 1：技术债一波清 — 消化 10 项 v0.7.0 P2 + 追加 CONTEXT.md

> ✅ **v0.7.0 已发布（2026-06-28）**。本阶段任务全部交付，详见 CHANGELOG `[0.7.0]` 段「阶段 1」。

> **独立会话指令**：`阅读 CONTEXT.md 和 docs/stages/v0.7-stage-1.md，完成所有任务后确认完成`
>
> **开工前只需阅读**：`CONTEXT.md`、`docs/stages/v0.7-stage-1.md`、`docs/tech-debt.md`、测试命令。**不需要读其他阶段**。

**目标**：消化 `docs/tech-debt.md` v0.7.0 候选 11 项中的 10 项小修（TD-6 大重构留阶段 5 独立做，TD-11 决策类归档不修）。所有改动都是局部的、surgical 的，不引入新依赖、不改架构。同步追加 CONTEXT.md 的 v0.7.0 新术语段（已由 plan.md 设计阶段写入，本阶段验证完整性）。

**前置依赖**：v0.6.0 已 tag（`git tag v0.6.0`），主干 master 在 `219f109` 提交。

**依赖测试**（开工时跑这些测试的详情，验证 v0.6.0 baseline）：

- `cargo test --release -q`（全量 summary，应 ≥ 701 passed / 0 failed / 3 ignored）
- `cargo test --release --test test_inspector -- --nocapture`（v0.6.0 键位兼容基线）
- `cargo test --release --test test_platform_compat -- --nocapture`（Linux 降级路径）

**任务清单**：

1. **TD-1 删 `--tb=no` 文档（5 min）**
   - 文件：`CONTRIBUTING.md:28` / `plan.md`（已是新版，但旧 stage 文件还有）/ `docs/stages/stage-{2,3,4,5,6,7}.md` 多处
   - 命令：`grep -rn "--tb=no" .`（应只在文档命中，不在代码命中）
   - 全部删除 `--tb=no` 字符串，保留 `cargo test --release -q`
   - 验证：`grep -rn "--tb=no" .` 无结果

2. **TD-2/3 stage-6/7.md 修订推迟标注（10 min）**
   - `docs/stages/stage-6.md:5`「目标」段，加 ⚠ 标注："v0.6.0 实际只做了任务 1/2（键位修复），任务 3-5（proptest/criterion/Linux stub）推迟到 v0.7.0+ — 见 CHANGELOG"
   - `docs/stages/stage-7.md:59-60` 切片 E 改问"是否需要引入 proptest（v0.7.0+ 评估）" / "性能回归测试是否覆盖 hot path"（原问题假设 proptest/criterion 存在）
   - 验证：两个文件头部含推迟声明

3. **TD-4 `WorkerManager::restart` 显眼标注（10 min）**
   - `CONTEXT.md:55` 已经在表格里说"未实现"，但藏在中间不够显眼
   - 改造：在 CONTEXT.md「后台 worker」段顶部加"⚠ 已知限制"小段，列出 `restart(name)` 未实现 + worker 崩溃后只能重启 proc
   - 同步在 `README.md` FAQ 段加一行"worker 崩溃了怎么办"（如果还没有的话）
   - 验证：grep `WorkerManager::restart` 应在 CONTEXT.md / README.md / `src/workers/manager.rs` 都有显眼标注

4. **TD-5 `proc diag` 补 Docker worker 行（1-2 h）**
   - 现状：`src/cli/diag.rs:25` 输出不含 Docker logs worker（DockerPanel 自管）
   - 步骤：
     - 在 `src/view_models/docker_panel.rs` 加 `pub fn metrics(&self) -> Vec<NamedWorkerStats>`（可能多个 logs worker，按 container_id 命名 `docker_logs_<container>`）
     - 在 `src/app.rs::worker_metrics`（或同名方法）追加 Docker 行
     - 注意 DockerPanel 可能 spawn 多个 logs worker，需聚合或全列
   - 测试：`tests/test_diag.rs`（已存在的话）加 case 验证输出含 `docker_logs_*`
   - 验证：`cargo run --release -- diag --json` 输出含 Docker 行

5. **TD-7 test_stage8_perf_regress.rs 改名（1 min）**
   - 当前 `tests/test_stage8_perf_regress.rs` 文件注释写"Stage-8 一次性"，但实际是 stage-4 落地时一起写的
   - 改名：`git mv tests/test_stage8_perf_regress.rs tests/test_perf_baseline.rs`
   - 更新文件头注释：从"Stage-8 一次性性能回归基线"改为"v0.6.0 阶段 4 性能基线（ProcessInfo 字段对齐）"
   - 全仓 grep 是否有其他地方引用旧文件名（如 CI yml）
   - 验证：`grep -rn "stage8_perf" .` 应只在 git history 出现

6. **TD-8 help_panel Workers 区段自适应列宽（1-2 h）**
   - 现状：`src/tui/help_panel.rs:156-178` 硬编码 `name<10` `avg>5μs` `max>5μs` `polls>7` `drops>5`
   - 修复方案：worker 名 truncate 到 10 字符 + ellipsis（`…`），或改用 ratatui `Table` widget 替代 `Paragraph` + 手工对齐
   - 推荐：先实现 truncate（最小改动），如果还是不对齐再换 Table
   - 测试：`tests/test_help_panel.rs`（已存在）加 case：worker 名 14 字符时表格列对齐无错位
   - 验证：TUI 启动后按 `?` 查看帮助页 Workers 区段

7. **TD-9 SearchState 增量 lowercase append（1-2 h）**
   - 现状：`src/search.rs:50-56` 每按键 `self.query_lower = self.query.to_lowercase()` 整体重算
   - 修复：
     - `Char(c)` 分支：`self.query_lower.push(c.to_ascii_lowercase())`（c 已经是 char，O(1)）
     - `Backspace` 分支：`self.query_lower.pop()`
     - Unicode 复杂大小写（`İ`→`i̇` 等多字节映射）检测：c.is_ascii() 时走增量路径；非 ASCII 时走整体重算 fallback
   - 测试：`tests/test_search.rs`（已存在）加 case：query=64 ASCII 字符时 lowercase 耗时 < 整体重算 1/10；query 含 `İ` / `ı` 时大小写正确
   - 验证：搜索框逐字符输入累积延迟从 μs 级（v0.6 baseline）到 ns 级（不严格要求，只要不退化）

8. **TD-10 DNS PowerShell probe 走 restricted_spawn（1-2 h）**
   - 现状：`src/dns_log/windows_dns.rs:238` probe `Command::new("powershell.exe")...status()` 未走 restricted_spawn
   - 修复：probe 改用 `crate::security::restricted_spawn::spawn_with_reduced_privileges` + `.wait()` 替代 `.status()`
   - 注意：probe 在主 spawn 之前调用，验证 restricted_spawn 模块此时已可访问
   - 测试：`tests/test_dns_log.rs`（已存在）加 case：probe 调用后 SeDebugPrivilege 已剥离（如可测）
   - 验证：`grep -n 'Command::new("powershell' src/dns_log/` 应只在 restricted_spawn fallback 路径出现

9. **TD-14 panic hook chain 集成测试（半天）**
   - 现状：`src/main.rs:60-61` 先 `init_tracing` 再 `install_panic_hook`；`tui::setup_terminal` 用 `take_hook` chain。链条从未端到端验证过
   - 修复（**不是改代码，是写测试**）：`tests/test_panic_hook_chain.rs`
     - 测试 1：调用 `install_panic_hook()` → 模拟 setup_terminal 也注册 hook → 用 `std::panic::catch_unwind` + 手动构造 `PanicHookInfo`（或调一个 `panic!` 宏 + catch_unwind 捕获）→ 验证：
       - `~/.config/proc/crashes/crash-{ts}.txt` 出现（用 tempdir 注入路径）
       - 文件包含 panic location + version + backtrace
       - terminal restore 被调用（用 mock terminal 或 AtomicUsize 计数器）
     - 测试 2：worker panic 走 `catch_unwind`，不进 panic hook 链（Rust 标准库语义验证）→ worker panic 后 `crashes/` 下出现 `crash-worker-{name}-{ts}.txt`（v0.6.0 阶段 8 P1-7 已实现）
     - 测试 3：CLI 模式（无 TUI）panic 时，crash report 也写盘（setup_terminal 未调用，链条只有 crash hook 一层）
   - 验证：3 个测试全部通过

10. **追加 CONTEXT.md 新术语完整性验证（5 min）**
    - 验证 CONTEXT.md 已含 v0.7.0 新增术语段（阶段 0 已写入）
    - 检查每个新术语（McpServer / CommandPalette / FilterExpr / PanelController / PsiStats / EcoQoSState / DiskIoStats / ProcessFlow / EbisuBpfWorker / SecurityRule R15）都有完整定义 + 代码位置
    - 如有缺失，补全
    - 验证：`grep -c "v0.7 阶段" CONTEXT.md` 应 ≥ 10（10 个新术语）

11. **归档 TD-11 决策类（5 min）**
    - `docs/tech-debt.md` TD-11（watchdog spawn restricted_spawn）添加"决策：不修。理由：用户主动配置的命令威胁模型不同，可能依赖 elevated token；v0.8.0+ 可加 config 选项 `inherit_privileges`"
    - 把 TD-1/2/3/4/5/7/8/9/10/14 标记为 "✅ Fixed in v0.7.0 阶段 1"
    - 验证：`grep -c "Fixed in v0.7.0 阶段 1" docs/tech-debt.md` 应为 10

**验收标准**：

- `cargo test --release -q` 全部通过（≥ 701 + 至少 5 新测试 = 706+ passed / 0 failed）
- `cargo fmt --all -- --check` 通过
- `cargo clippy --release --all-targets -- -D warnings` 0 warnings
- `cargo build --release --no-default-features` 编译通过
- `grep -rn "--tb=no" .` 无结果
- `grep -rn "stage8_perf" .` 只在 git history 出现
- `proc diag --json` 输出含 Docker worker 行
- TUI 启动后按 `?` 帮助页 Workers 区段视觉对齐
- `~/.config/proc/crashes/` 目录在 panic 后正确写盘（集成测试覆盖）
- CONTEXT.md 含完整 v0.7.0 新术语段（10 个新术语）
- `docs/tech-debt.md` 10 项 v0.7.0 候选标记 Fixed，TD-11 标记决策不修

**容量预估**：约 200-400 行修改（不含测试） + 3-5 个新测试文件 ≈ 600-800 行总改动，工具调用 ≤ 50 次，远低于 1500 行 / 100 次上限。
