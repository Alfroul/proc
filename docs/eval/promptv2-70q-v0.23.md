# prompt v2 实验 70 query 对比归档（v0.23 stage 3）

> ADR-0033 D1 实际形态 2 列矩阵实跑（2026-08-23）：prompt v2（缺参引导 + 写操作发现链，附录 A 措辞稿）vs v1 基线 + 同配置复跑方差列。结果 JSON（`eval-promptv2-70q.json` / `eval-best-70q.json`）本地留存不入 commit，本 md 是精炼归档。
>
> **拍板结论（D4 保守）**：v2 无明确改善——净通过增益落在 E2B 方差带内、L2 一致回退、修订 1 直接靶点未动。**revert 回 v1 维持默认**（commit `c043597`）；修订 2（写操作发现链）机制单独生效，记 v3 候选推 v0.24+。

## run 元信息

| 项 | 基线（v1） | 列 ①（v2） | 列 ② best（v2 复跑） |
|---|---|---|---|
| run（UTC） | 2026-08-22T02:46:12Z | 2026-08-23T12:11:41Z | 2026-08-23T13:19:04Z |
| git | `v0.21.0-5-gb210dc3` | `v0.22.0-4-g5d2ac64` | `v0.22.0-4-g5d2ac64` |
| 时长 | 47m 19s | 37m 07s | 38m 43s |
| 输出 | `eval-e2b-70q.json`（v0.22 归档） | `eval-promptv2-70q.json` | `eval-best-70q.json` |

provider/参数三列一致：llama-cpp（llama-server b8685 CUDA 12.4 + gemma-4-E2B-it-Q4_K_M），attempts=2，max_steps=10，70 query（L0 23 / L1 27 / L2 20），GBNF off（grammar_file 注释态）。**环境一致性**：Docker daemon 三轮均未运行（docker 场景通过 1/10 / 2/10 / 2/10，final_text 均为「Docker 未运行」应对——单变量隔离成立）。列 ①② 的 `git_describe` 同 hash（D2：v2 落地在独立 commit `5d2ac64`，与基线列天然区分）。

## 对比矩阵

| 指标 | 基线 v1 | v2 列① | v2 列② | v2 均值 | 判读 |
|---|---|---|---|---|---|
| L0 | 17/23（74%） | 17/23 | 19/23（83%） | 18 | 噪声带内（列①平、列②+2） |
| L1 | 14/27（52%） | **16/27（59%）** | **16/27（59%）** | 16 | **两轮一致 +2**（唯一稳定正向） |
| L2 full-chain | 1/20（5%） | 0/20 | 0/20 | 0 | **两轮一致 -1** |
| L2 chain-step | 12/43（28%） | 12/43 | 13/43（30%） | 12.5 | 基本持平 |
| output_degraded | 21 | 16 | 19 | 17.5 | 两轮均低于基线，自差 3 |
| wrong_tool | 10 | 11 | 5 | 8 | 自差 6——直方图轮间漂移最大项 |
| chain_incomplete | 7 | 10 | 11 | 10.5 | **两轮一致 +3/+4**（L2 回退的镜像） |
| 净通过 | 32 | 33 | 35 | 34 | +1/+3，方差带边缘 |

失败模式迁移（基线→列①/列②）：output_degraded 21→16/19（-5/-2）；wrong_tool 10→11/5（+1/-5）；chain_incomplete 7→10/11（+3/+4）。退化减少的构成：部分翻成通过（3 条 / 8 条），部分迁移成其他失败模式——「失败质量」与「失败转移」混合，非全净收益。

## 方差观察（列① vs 列②，同 binary 同配置——E2B 非确定性首次跨 run 量化）

- **6 条 query 纯因非确定性翻转**（4 升：idx16/34/42/56，2 降：idx14/46）——v0.22 观察 2「同 query 两次 attempt 可不同」的 run 级放大
- **方差带标尺**：单次 run 的 ±3 通过数 / ±6 失败模式计数在噪声内——未来所有 `--compare` 单列差异小于此量级时不可单独归因（v0.24 模型底座决策的解读标尺）
- 逐 query 三轮稳定性（13 条有差异）：**一致改善 4**（idx23 docker 镜像 / idx44 PID 1234 / idx59 chrome 远程 / idx63 监控添加）、**一致回退 2**（idx20 签名验证 full-chain / idx57 非标端口）、摇摆 7

## 靶点 query 逐条（附录 A 两修订的定性 verdict）

**修订 1（缺参引导——治基线 #19/#25/#26/#29 反问缺参）**：

| 靶点 | 基线行为 | 列① | 列② | verdict |
|---|---|---|---|---|
| #19 idx37 USB 分步诊断 | 反问盘符 | 仍反问 | **走枚举诊断链**（proc_help→metrics→eject_status） | 1/2 轮迁移 |
| #25 idx47 %TEMP% 未签名 | 解释局限 | 多轮尝试（含 proc_export） | filter 解析失败退化 | 未迁移 |
| #26 idx48 可疑进程画像 | 反问 PID | 仍反问 | 仍反问 | 未迁移 |
| #29 idx52 录屏异常事件 | 英文反问路径 | 中文反问 + `<eos>` 循环 | 反问 + 退化 | 未迁移 |

两轮合计 1/4 靶点迁移——**修订 1 基本未生效**。根因分析：靶点场景缺真正的无参枚举 tool（recording 无「列出录屏文件」、`proc_eject_status` 需盘符参数）——prompt 引导「先枚举」但枚举工具不存在时模型仍回退反问；#20/#22 等场景模型确实枚举了 A:-U: 全盘符，说明引导语义被理解，只是靶点 query 无处落地。

**修订 2（写操作发现链——治基线 #21 直接声明不可执行）**：

| 靶点 | 基线行为 | 列① | 列② | verdict |
|---|---|---|---|---|
| #21 idx39 反馈循环演示 | 零 tool 调用 + 角色扮演「不能执行」 | proc_help 发现后再反问盘符 | **完整执行演示链**（proc_help→proc_eject→proc_eject_status，blocked 后继续） | **两轮递进迁移** |
| idx18 释放 E 盘 | 退化 | 调用→blocked→解释+等价命令行（外壳仍退化） | — | 行为符合措辞预期 |
| idx63 监控添加 | 退化 | 真实执行 proc_monitor_add×2 + metrics 验证（过） | 过 | **两轮一致通过** |

修订 2 的「先调用、blocked 后解释」机制**可复现地生效**（3 处证据）——单独构成 v3 候选（去掉未生效的修订 1，避免其 L2 回退嫌疑），推 v0.24+ 与 RAG cycle 一并评估。

## 拍板（D4 标准，用户 2026-08-23 拍板 A）

1. **主指标**：L0/L1 合计 +2/+4（一轮低于、一轮高于 ±3 阈）；output_degraded -5/-2（一轮达标一轮不达标）——均值落在阈值边缘，且列①②自差（degraded 3 / wrong_tool 6）超出 ±2 稳定带 → **方差主导，增益不成立**
2. **L2 一致回退**（full-chain 1→0→0 + chain_incomplete +3/+4 两轮同向）——多步规划受损有真信号嫌疑，与修订 1「先枚举别反问」在多步链中引入额外决策分叉的假设相容（未证实）
3. **保守拍板**：无明确改善不动默认 → `git revert 5d2ac64`（commit `c043597`），system.md 终态 = v1
4. **实验价值归档**：prompt 措辞变量用数据关闭（REVIEW-v0.22 候选 4 兑现）；E2B 方差带量化（±3/±6 标尺）；修订 2 有效性证据 + v3 候选记录

## 与 GBNF 结论（附录 B）的合并视图

v0.23 两条候选修复路径全部实验完毕：GBNF 结构性不可用（llama-server b8685 拒绝 grammar×tools 同传）；prompt v2 无明确增益（本归档）。E2B 画像（L0 74% / L1 52% / L2 full-chain 5% / degraded 30%）维持 v0.22 基线——**进一步改善的杠杆只剩模型升级**（更强模型对比列推 v0.24+，brainstorm 决策 2 既定排期），output_degraded 21 次主靶手随 GBNF 关闭暂时无零代码解。
